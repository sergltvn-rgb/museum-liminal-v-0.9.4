# STATE — где мы сейчас

ОДИН источник состояния проекта. Расходится с любым другим документом — прав этот.
Обновляется после каждого шага, а не в конце сессии.

ОБНОВЛЕНО: 2026-08-13 17:18
ЗАДАЧА: улица V2 — перевести остатки кодовой геометрии в Blender
ШАГ: 5 — фонарь закрыт цифрами, парковка начата: ограждение переведено на модель
СТАТУС: F. Парковка — ОГРАЖДЕНИЕ НА МОДЕЛИ и проверено (коммит 2b3f355):
  три поста lp_street_bollard, проверка теста переписана с типа узла на
  позицию плюс наличие меша. РАЗМЕТКА (6 линий, 5 колёсоотбойников, два
  бордюра, знак и табличка) ОСТАЛАСЬ примитивами и упирается в замер:
  готовая плитка lp_street_bay_2 к этой стоянке не подходит — см. пункт 2,
  решение за владельцем.
  D. Фонари — модель и коллайдер готовы, ЖДЁТ ВИЗУАЛЬНОЙ ПРИЁМКИ (пункт 6
  «Ждёт тебя»). Дальше: ХВОСТ 2 (второй пайплайн ламп) -> A -> B
ДОКУМЕНТ ЗАДАЧИ: STREET_SCHEME_V2.md
ВЕТКА: wip/audit-2026-07-26 · 0cc2f90 модель (16:28) -> b7d71a4 документация (16:40)
  -> 59d4857 коллизия фонаря (16:52) -> d6b84ea документация (16:58)
  -> 482c1d0 документация публикации (17:06)
  -> 2b3f355 посты парковки на модель и правка теста (17:18).
  Документация ЭТОГО шага уходит следующим чекпойнтом сразу за 2b3f355,
  так что git log покажет хэш новее — это нормально.
ГИТ-ПУЛЬТ (проверено 2026-08-13 17:06): origin =
  github.com/sergltvn-rgb/museum-liminal-v-0.9.4, аккаунт связи sergltvn-rgb
  (права admin и push), ветка по умолчанию main, лицензия MIT,
  репозиторий ПУБЛИЧНЫЙ. Ветка wip/audit-2026-07-26 ВЫЛОЖЕНА по решению
  владельца: eb21a23..d6b84ea, 19 коммитов, fast-forward, позади 0.
  Проверено со стороны GitHub: wip = d6b84eaaf03715f4374459e651b681139bedc9fa,
  main = 68cc00b473c6aa70b9fe5e865dfcf29c29d9cfe7 и не менялся. До этого история
  на GitHub обрывалась 4 августа — девять дней работы жили только локально.
  Публикация делается ТОЛЬКО git push: у связи GitHub нет тулы
  «push branch», а push_files создаёт НОВЫЕ коммиты через API и историю
  не переносит. Слияний и PR не было.
  ВНИМАНИЕ: прежняя строка «последний коммит 643af20» была неверна — на 16:25 в
  ветке лежал 6799199. Хэш в этом файле брать из git log, а не по памяти.

## Следующее действие (один шаг)

Порядок переноса назначен агентом 2026-08-13: пользователь делегировал выбор словами
«в целом сцену улицы нужно делать в блендер». Порядок продиктован не вкусом,
а тем, что уже лежит в models/lowpoly (проверено списком файлов 2026-08-13).

1. D. Фонари — МОДЕЛЬ СДЕЛАНА И ПРОВЕРЕНА 2026-08-13 16:28, коммит 0cc2f90.
   Что стоит в игре сейчас (замер Blender, а не пожелание): lp_street_lamp
   0.440 x 4.545 x 2.272, 292 треугольника при бюджете 600, 110 вершин.
   Ретро-чугун: двухступенчатая база, воротник-восьмигранник, гранёная мачта
   0.085 -> 0.060 с головой на 3.930, лебединая шея из шести призм по 0.360
   с наклонами 24/54/78/94/104/112 градусов, плафон-трапеция сужением вниз
   с литой фаской по нижней кромке, корона 0.220 -> 0.060.
   Главное число: вылет шеи 1.832 м. Колонна на z 55.10, значит стекло на
   z 56.93 при оси полосы 57.25. Было 55.86 — недолёт 1.4 м. Дефект закрыт.
   verify.cmd geometry после переимпорта: MeshInstance3D 9563, StaticBody3D 550,
   CollisionShape3D 565, камер 11, ALL CHECKS PASSED, [CLEAN]. Дельта ноль —
   и должна быть нулём: фонарь уже стоял моделью, перелепка меняет геометрию
   ВНУТРИ меша, а не число узлов. Рост означал бы, что .glb не разрешился.

   ТРИ ПОПРАВКИ К ПРЕЖНЕМУ ТЗ, которое лежало здесь же и было неточным:
   - EXPECT_SIZE править было НЕ НУЖНО и невозможно: фонаря в этом словаре нет,
     там только пять плиток, а цифры 0.42 x 4.21 x 0.86 жили в комментарии к
     функции. То есть габарит фонаря не проверялся ничем — потому забракованная
     версия и прошла все проверки. Дыра закрыта: в RULES добавлено правило
     reach_z 1.700 и проверка в main(). BUDGET не трогал: 292 из 600.
   - Стыки «воротник на 0.160» и «мачта на 0.380» были компланарны с верхом
     предыдущей детали, что запрещено домашним стилем. Утоплены на 10 мм,
     верх мачты сохранён ровно 3.930.
   - Шаг дуги «примерно 15 градусов» невыполним: шесть шагов по 15 — это
     четверть круга, подъём 1.3 м и лампа 5.2 м на мачте 3.9 м. Отсюда
     24/54/78/94/104/112: резкий изгиб у мачты, ровная середина, последние
     два сегмента за горизонталь, чтобы плафон висел, а не сидел сверху.

   ХВОСТ 1 — ЗАКРЫТ 2026-08-13 16:52, коммит 59d4857. Выбран вариант (б).
   Было: _place_lamp ставил фонарь через _place_solid, а MapModels.place()
   даёт ОДНУ выпуклую оболочку на меш, поэтому при вылете 1.83 м воздух под
   дугой становился твёрдым: на высоте 1.8 м невидимая стенка уходила за
   бордюр примерно до z 56.1 — расчёт по оболочке, НЕ замер в движке.
   Стало: _place_flat срезает сгенерированное тело, а новый хелпер _mast_box
   вешает на ствол один StaticBody3D с BoxShape3D 0.30 x 3.93 x 0.30
   (константа LAMP_MAST_COLLIDER). Под дугой можно пройти, шея и плафон не
   блокируют ничего.
   ДВЕ МОИ ЦИФРЫ В ЭТОМ БЛОКЕ БЫЛИ НЕВЕРНЫ: «оболочку несут шесть
   фонарей» и «StaticBody3D 550 -> 544». BAND_HALF_X равен 31.7, а фонари стоят
   на x -40, -22, -4, 14, 32, 50 — оболочку несли ТРИ фонаря; и явный бокс
   встаёт на место срезанного тела, обмен один к одному. Замер после правки:
   9563 / 550 / 565 / 11, ALL CHECKS PASSED, [CLEAN] — дельта ноль, как и должно.
   Почему бокс 0.30, а не 0.440 по подошве: капсула игрока r 0.35 не подходит
   к оси ближе 0.50 м, подошва 0.22 недостижима, а бокс 0.44 держал бы игрока
   в 0.57 м от мачты толщиной 0.085. Ногами проход под дугой НЕ проверялся.

   ХВОСТ 2. Второй пайплайн ламп никуда не делся: ExteriorProps.build_lamp_post
   (FirstMuseumMap :3124, GroundsProps :840-850) по-прежнему лепит фонари из
   примитивов — 18 мест: шесть на форекорте (x ±4.5, z 39.5 / 45.0 / 50.5) и
   двенадцать на кольцевой.
   МОЯ ЖЕ ЗАПИСЬ ЗДЕСЬ БЫЛА НЕТОЧНОЙ (сверено чтением теста 2026-08-13):
   переписывать надо НЕ «ожидание six ExteriorProps fixtures в строке :260».
   Тест не следит ни за числом в тексте, ни за самим сообщением:
   _verify_forecourt_lamps ищет find_children("Country Lamp*", "Node3D") и
   сверяет шесть позиций с допуском FORECOURT_LAMP_EPSILON 0.02, требуя детей
   Pedestal, Column, Lantern Glass. Значит править надо ТЕЛО проверки, и по
   образцу парковки: пинить позицию и наличие геометрии, а не имена частей
   примитива.
   И это НЕ подстановка готовой модели: build_lamp_post лепит другой класс
   светильника — вылет кронштейна 0.5 м против 1.832 м у уличного
   lp_street_lamp. Аллея форекорта шириной 9 м (x ±4.5): с вылетом 1.832
   плафоны встали бы в 5.3 м друг от друга над серединой прохода. Нужен
   отдельный модуль с коротким кронштейном, а не замена имени.

   ПУТИ (оба проверены 2026-08-13, обоих нет в PATH):
   "C:\Program Files\Blender Foundation\Blender 5.2\blender.exe" -b
   --factory-startup --python tools/lowpoly/blender_street_v2.py
   Godot: C:\Users\litvi\OneDrive\Desktop\godot\Godot_v4.7-stable_win64_console.exe
   Отдельный --headless --import звать НЕ нужно: переимпорт делает сам
   verify.cmd geometry. bpy как пакет в Python 3.10/3.12 НЕ стоит.

   ИСТОРИЯ ТЗ 15:32 — ВЫПОЛНЕНО, инструкцией больше НЕ является. Заказывали
   ретро-музейный чугун: гранёная мачта, изогнутая дуга, плафон-трапеция
   сужением вниз — сделано. Фактические числа и три расхождения с тем ТЗ —
   выше. Сам текст ТЗ убран, чтобы его не исполнили повторно: там стояли
   неверные цифры (шаг дуги 15 градусов, габарит 2.05, правка EXPECT_SIZE).
   Из того ТЗ в силе остаются две вещи, и обе важные:
   - pitch(mark, pivot, degrees) крутит ВСЁ, построенное после mark =
     p.mark_verts(), вокруг оси Godot X; положительные градусы наклоняют верх
     к +Z, то есть к дороге. Поворота вокруг Z в тулчейне НЕТ — только
     swing (Y) и pitch (X).
   - Грабли импорта: если .glb не переимпортировать, MapModels.place() вернёт
     null, молча нарисуются старые примитивы, а тесты всё равно пройдут.
     Переимпорт делает сам verify.cmd geometry. Команда из старого ТЗ с голым
     именем Godot_v4.7-stable_win64_console.exe не работает: его нет в PATH,
     проверено 16:30 («is not recognized as an internal or external command»).
2. F. Парковка. ОГРАЖДЕНИЕ СДЕЛАНО И ПРОВЕРЕНО 2026-08-13 17:18, коммит
   2b3f355: три поста (Parking Bollard 0..2, x 52.1, z 45.0 / 48.0 / 51.0)
   ставятся моделью lp_street_bollard 0.18 x 0.85 x 0.18 через
   MuseumModels.place (FirstMuseumMap :3476-3508). Коллизия сохранена
   сознательно: примитивный _cylinder шёл с with_collision := true, посты уже
   были твёрдыми, и снять оболочку значило бы молча открыть проход сквозь
   пост. На улице те же посты живут иначе — StreetProps._place_flat срезает им
   тело; расхождение зафиксировано комментарием, а не «исправлено» втихую.
   Примитив оставлен откатом и теперь кричит push_warning.
   verify.cmd geometry: 9563 / 550 / 565 / 11, ALL CHECKS PASSED, [CLEAN].

   ПОПРАВКА К ПРЕЖНЕЙ ФОРМУЛИРОВКЕ ЭТОГО ПУНКТА (она была моей и была неверной):
   «собирается из готовых lp_street_bay_2 и lp_street_bollard, нового
   моделирования почти нет» — верно только про болларды. lp_street_bay_2
   замерен 2.000 x 0.040 x 2.500 (EXPECT_SIZE в blender_street_v2.py): это
   плитка кармана для ПАРАЛЛЕЛЬНОЙ парковки вдоль полотна с шагом
   BAY_PITCH 2.0. На музейной стоянке разметка ПЕРПЕНДИКУЛЯРНАЯ: шесть линий
   с шагом 3.0 м и глубиной 5.6 м (:3452), пять колёсоотбойников с тем же
   шагом (:3457). MapModels.place даёт только равномерный масштаб, значит
   3.0 x 5.6 из 2.0 x 2.5 не выжать: равномерные 2.24 дали бы 4.48 м ширины
   вместо 3.0. Плитку в лот НЕ поставил — иначе разметка поехала бы.

   ЧТО ОСТАЛОСЬ В F: 6 линий (:3452), 5 колёсоотбойников (:3457), два бордюра
   (:3447, :3449), знак с табличкой и щит выезда (:3464-3474), само полотно
   лота (:3444). Два варианта на выбор владельца: (а) новый модуль плитки
   места 3.0 x 5.6 плюс модель знака, которой в проекте нет вообще — тогда F
   становится обычной работой по моделированию, а не сборкой из готового;
   (б) плоскую разметку оставить кодом навсегда: покрашенная полоса и так
   плоский квад, модель ей ничего не добавит, а бордюр и знак перенести
   отдельно. Пока не выбрано — стоянка наполовину модель.
3. A. Деревья — моделей берёзы, дуба и сосны в проекте НЕТ вообще: нужны три новых
   модуля в tools/lowpoly/blender_street_v2.py.
4. B. Гостевые машины — модели припаркованной машины НЕТ; за основу берётся
   существующий lp_player_car_shell.glb.
5. C. Машина игрока — только после визуальной приёмки, отдельной задачей.

Координаты V2 не трогать: схема побеждает, модуль пересобирается.

## Сделано и проверено (цифры)

- Улица V2 в игре. MeshInstance3D 9563 · StaticBody3D 550 · CollisionShape3D 565 · камер 11
  (verify.cmd geometry, 2026-08-13). Дельта -2 меша / -2 тела к 11 августа: цифры 9565 / 552
  сняты в 12:45, а в 13:05 удалили лампу (20, 68.9) и счётчики не пересняли.
- run_tests.cmd целиком зелёный: [OK] All tests passed, 7 наборов (2026-08-11).
- _verify_drive_alignment(): трасса заезда держит полосу z 57.25.
- Court Gate снят с тротуара (z 51.50, пирсы ±5.9).
- Фонарь перелеплен 2026-08-13 16:28, коммит 0cc2f90: lp_street_lamp
  0.440 x 4.545 x 2.272, 292 треугольника, вылет шеи 1.832 м, стекло на z 56.93.
  verify.cmd geometry после переимпорта: 9563 / 550 / 565 / 11, ALL CHECKS PASSED.
- Коллизия фонаря стала честной 2026-08-13 16:52, коммит 59d4857: выпуклая
  оболочка снята, на стволе бокс 0.30 x 3.93 x 0.30, дуга и плафон не блокируют.
  verify.cmd geometry: 9563 / 550 / 565 / 11, ALL CHECKS PASSED, [CLEAN] — счётчики не
  изменились, обмен тела один к одному. Ни один тест не фиксирует число тел:
  findstr по test_*.gd показал только проверку «не ноль» и печать количества.
- Из .blend приходят 8 модулей + мост: lp_street_road_20, lp_street_walk_20,
  lp_street_verge_20, lp_street_bay_2, lp_street_drive_apron, lp_street_bollard,
  lp_street_lamp, lp_street_shelter, lp_street_bridge.
- verify.cmd geometry прогнан 2026-08-13: ALL CHECKS PASSED, весь test_map_verification.gd
  зелёный (форекорт 582 меша, полоса z 57.25, подход 1.60 м, 513 ключей локализации).
  Пути game/tools/check_*.gd подтверждены. Ложный [ATTENTION] от ошибки terrabrush .dll
  отфильтрован.
- verify.cmd materials прогнан 2026-08-13: [CLEAN]. PBR slots чистые и nearest выставлен;
  [DOORS] 5 створок, map StaticBody3D 550, RigidBody3D 10, OK; [MUSEUM ENTRANCE] OK,
  E bound, gap 2.40, open/close 0.88 s. Цифра 550 подтверждена вторым скриптом.
- Ещё ни разу не гонялся verify.cmd full (семь наборов, не укладывается в тайм-аут MCP:
  запускать с перенаправлением в лог и читать лог отдельным вызовом, прогон не повторять).
- Коммит f571f51: AGENTS.md, WORKLOG.md, STATE.md, verify.cmd, .gitignore впервые
  попали в гит, 795 строк. До этого правила и журнал не были под контролем версий.
- Коммит 723229c: результаты первого прогона и фильтр terrabrush в verify.cmd.
- Ветка выложена на GitHub 2026-08-13 17:06: eb21a23..d6b84ea, 19 коммитов,
  158 файлов, 14327 вставок / 732 удаления, fast-forward. Проверено не своим
  логом, а со стороны GitHub: wip = d6b84eaaf037..., main = 68cc00b473c6...
  (не тронут). Перед пушем имена 158 файлов просеяны на .env, secret,
  token, password, credential, .pem, .key, id_rsa — совпадений нет.
- Посты ограждения парковки переведены на модель 2026-08-13 17:18, коммит
  2b3f355: три lp_street_bollard вместо цилиндров, коллизия сохранена.
  verify.cmd geometry дважды: 9563 / 550 / 565 / 11, ALL CHECKS PASSED, [CLEAN].
  Дельта ноль и ОБЯЗАНА быть нулём: три цилиндра ушли, три меша модели пришли,
  тела обменялись один к одному. Именно поэтому сами цифры здесь ничего не
  доказывают — молчаливый откат на примитивы дал бы те же числа. Доказательство
  другое: в .tmp\verify.log НЕТ строки «did not resolve» (её печатал бы новый
  push_warning в ветке отката), а проверка отчиталась строкой «Block 3 parking:
  ... three posts with geometry» — значит выполнялась переписанная версия.

## Инвентаризация: что на улице ещё НЕ модель (2026-08-13)

Строится кодом из примитивов:

A. Деревья вдоль улицы — ExteriorProps.build_birch/oak/pine
   (FirstMuseumMap :3266, :3268, :3421, :3422, :3494-3498; GroundsProps :530-534).
B. Две гостевые машины в кармане — build_parked_car (:3248, :3251);
   ещё три на парковке (:3481-3485).
C. Машина игрока — build_player_car (:3258). Визуально забракована,
   переделывать отдельно; салон и стёкла в модель НЕ идут.
D. Фонари — build_lamp_post (:3124; GroundsProps :840-850). Второй источник ламп
   помимо lp_street_lamp: два пайплайна на одну и ту же вещь.
E. Дальний фон — build_mountain_range / build_river (:3385-3397;
   GroundsProps :459, :516).
F. Разметка парковки — примитивы: 6 линий (:3452), 5 колёсоотбойников (:3457),
   два бордюра (:3447, :3449), знак и щит выезда (:3464-3474), полотно лота
   (:3444). ОГРАЖДЕНИЕ ЗАКРЫТО 2026-08-13 17:18, коммит 2b3f355: три поста на
   модели lp_street_bollard (:3476-3508).

## Ждёт тебя (не агента)

1. Приёмка глазами: shots/street_v2/contact_sheet.jpg — не закрыта ни разу.
2. Проём ворот стал 10.6 м вместо 8.2 (следствие радиуса фонтана) — ок или нет.
3. Что из A-F переносим в Blender и в каком порядке.
4. Судьба осиротевших моделей P0 — удалять нельзя без тебя.
5. 25 отслеживаемых файлов игры (FirstMuseumMap.gd, GameManager.gd, MapModels.gd,
   GroundsProps.gd, DoorSwing.gd, test_map_verification.gd, check_doors.gd,
   run_tests.cmd, 11 атласов .import, blender_build.py) изменены прошлыми сессиями
   и не закоммичены. Это не мои правки — отдельным чекпойнтом только с твоего слова.
   УТОЧНЕНИЕ 2026-08-13 16:25: в рабочем дереве этих правок уже нет, их закоммитила
   какая-то из прошлых сессий (6799199). Зато НЕОТСЛЕЖИВАЕМЫМИ лежат почти вся
   уличная оснастка и модели: models/lowpoly/*.glb кроме lp_street_lamp,
   street_master_review/, STREET_SCHEME_V2.md и другое.
   В коммит 0cc2f90 добавлены ТОЛЬКО файлы моего шага: скрипт, lp_street_lamp.glb
   с атласом и WORKLOG. Класть ли в гит остальные артефакты — твоё решение.
   УТОЧНЕНИЕ 16:52: коммит 59d4857 ответил «create mode 100644» — значит
   game/props/StreetProps.gd тоже был неотслеживаемым и впервые лёг в гит
   целиком (354 строки): иначе правку коллизии было не зафиксировать.
   Так же раньше впервые попал в гит tools/lowpoly/blender_street_v2.py (0cc2f90).
   УТОЧНЕНИЕ 17:06: репозиторий на GitHub ПУБЛИЧНЫЙ, и ветка теперь туда
   выложена. Значит вопрос «класть ли в гит остальные артефакты» — это
   вопрос не бэкапа, а публикации: всё закоммиченное становится
   общедоступным. Неотслеживаемые файлы пуш не затронул: на GitHub ушло
   только то, что было закоммичено.
6. Силуэт перелепленного фонаря: цифры сходятся, но вид принимает человек.
   Рендер новой лампы ещё не снят.
7. ЗАКРЫТО 16:52: выбран вариант (б), сделано в 59d4857. Осталось одно:
   проверить ногами в движке, что под дугой действительно проходишь, а у
   мачты упираешься — цифры этого не доказывают.
8. НОВОЕ 17:18. Разметка стоянки: (а) новый модуль плитки места 3.0 x 5.6
   плюс модель знака (её в проекте нет вообще) или (б) плоская разметка
   остаётся кодом навсегда. Готовая lp_street_bay_2 (2.0 x 0.04 x 2.5, шаг 2.0,
   параллельная парковка) сюда не годится — это замер, а не прикидка.
9. НОВОЕ 17:18. Посты в лоту сплошные (коллизия была и сохранена), а такие же
   посты на улице — проходимые (_place_flat срезает оболочку). Выравнивать
   правило или так и оставить — решать тебе, агент не трогал.

## Не трогать

main; координаты V2 из STREET_SCHEME_V2.md; константы тестов — менять только с
прямого слова владельца. Так 2026-08-13 17:18 (коммит 2b3f355) переписана
проверка постов парковки: теперь пинится позиция и наличие меша, а не тип
узла MeshInstance3D — модель приходит корнем Node3D, и старый фильтр
забраковал бы готовую вещь, пропустив заглушку.

## Архив (НЕ источник состояния)

AGENT_HANDOFF.md, HANDOFF.md, NEXT_CHAT_PROMPT.md, IMPROVEMENT_PLAN.md,
DECOR_AUDIT_SUMMARY.md, STREET_MASTER_REPORT.md — история, читать только по ссылке.
