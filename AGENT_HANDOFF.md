# AGENT_HANDOFF.md — передача дел следующему агенту

**Дата последнего обновления:** 2026-08-10
**Ветка задачи:** low-poly декор + аудит положения пропов (museum-liminal v0.9.4)
**Статус:** работа НЕ завершена.

> **ВНИМАНИЕ, 2026-08-09: этот файл больше НЕ единственная точка входа.**
> Живая очередь работ и текущая директива пользователя переехали в два места:
> **`NEXT_CHAT_PROMPT.md`** (короткий, читать первым) и раздел **«Модели и декор»**
> в конце **`AGENT_PLAN.md`** (перепись примитивов, приоритеты, открытые риски).
> Разделы 0 и 6 ниже описывают заход 2026-08-06 и УСТАРЕЛИ — читай их как историю,
> а планы бери из двух файлов выше. `DECOR_AUDIT_SUMMARY.md` — итоги ещё более
> раннего этапа аудита.
>
> Что в этом файле по-прежнему ценно и нигде не продублировано: раздел 2 (как вообще
> смотреть картинки), раздел 3 (как снимать кадры), раздел 7 (грабли), раздел 10
> (якоря в коде) и таблица открытых дефектов D1 / D2 / D6 / D10 / D11 в разделе 5.

---

## 0-бис. Пакет 3 (2026-08-10) — архив, ротонда и лобби, четыре задачи закрыты

Третий тематический Blender-файл — `tools/lowpoly/blender_archive.py` (та же схема, что в
`blender_core.py`: `BUILDERS` / `BUDGET` / `RULES` и `main()`, который сам роняет сборку при
нарушении пола, центровки, высоты или бюджета трисов).

| модель | габарит W×H×D | tris | байт | коммит |
| --- | --- | --- | --- | --- |
| `lp_stack_carriage` | 0.940 × 2.201 × 3.140 | 366 | 25 924 | `841c90b` |
| `lp_rotunda_bench` | 3.280 × 1.030 × 0.692 | 344 | 25 920 | `1a97c1d` |
| `lp_lobby_counter` | 4.840 × 1.070 × 3.235 | 208 | 16 396 | `0925a63` |

Все три модели в `MapModels.NON_BLOCKING` (коллайдеры ставятся вручную в обеих ветках),
у каждой есть примитивный фолбэк с тем же именем узла (`_stack_carriage_shell`,
`_rotunda_bench_parts`, `_reception_counter_joinery`). Третьей задачей пакета Blender не
понадобился: `LobbyProps.build_reception_counter` теперь ставит готовые `lp_desk_monitor`
и `lp_keyboard` из пакета 2 (коммит `d614982`, фолбэк `_reception_monitor_parts`).

Четвёртая задача — столярка той же стойки: 15 примитивов из 31 ушли в
`lp_lobby_counter` (коммит `0925a63`, − 14 мешей, новый хелпер `LobbyProps._collider`).
**Плиты `Counter Ledge` и `Counter Wing Ledge` оставлены процедурными сознательно**:
на них живой 2K-набор `travertine` на высоте рук, плоский атласный цвет был бы
регрессом ровно там, куда смотрит игрок. Заодно выправлены два настоящих бага
геометрии: стойки фасции стояли ВНУТРИ полей (x −1.00 и +0.50 вместо швов на ±0.75)
и в одной с ними плоскости, а сами поля выступали на 15 мм вместо обещанных
комментарием «recessed» (в модели — `inset` на 12 мм вглубь). Все семь стыков разобраны
в шапке над `build_lobby_counter`.

**Машина игрока (`ExteriorProps.build_player_car`, 35 примитивов) в модель НЕ ИДЁТ** —
разведка сделана, вердикт отрицательный: это салон POV-катсцены, камера сидит
внутри (`DRIVER_EYE (-0.37, 1.08, 0.12)`, `FirstMuseumMap.gd:3855`); шесть прозрачных
примитивов на `COL_GLASS_TINT (0.10, 0.13, 0.16, 0.35)` и четыре эмиссивных (фары и
стопы 0.35, приборный диск 0.6, радио 0.4) — дом-стиль с одним непрозрачным атласом
их убьёт; геометрия наклонена под кадр (лобовое и A-стойка −28°, C-стойка +24°,
колонка и руль 65°, спицы через `rotate_object_local`). Максимум на будущее — внешняя
оболочка в модель, стёкла и салон процедурно, приёмка из кабины.

Счётчики после пакета: **`MeshInstance3D` 9986 → 9826 → 9734 → 9728 → 9714**,
**`StaticBody3D` 547 → 551 и дальше без изменений**, 11 камер CCTV, семь наборов
зелёные на каждом из четырёх коммитов. Кадры приёмки:
`shots/decor_audit/carriage/`, `/bench/`, `/lobby/` (лобби снято дважды — до и после
столярки). Приёмка по каретке, скамье и первому заходу на лобби получена от
пользователя глазами; кадры столярки ждут подтверждения.

---

## 0. Директива пользователя на 2026-08-06 (ЧАСТИЧНО УСТАРЕЛА, см. баннер вверху)

> **Актуально на 2026-08-09.** Пункт «хочу именно модели GLB, возможно через
> Блендер» подтверждён и усилен: пользователь сказал «делай пока лоу поли модели
> в блендер» — то есть блендерным конвейером (`blender_build.py` плюс тематический
> скрипт), а не чисто питоновым `build_props.py`. Планетарий и мониторы охраны
> отработаны. Мерцание пола и проёмов пользователь закрыл 2026-08-06 — не поднимать.
> Текущая очередь — в `NEXT_CHAT_PROMPT.md` и в конце `AGENT_PLAN.md`.
> Правило «не коммитить без разрешения» и требование судить только по реально
> открытому изображению остаются в силе.

Исходное ТЗ было большим (132 ракурса, audit.tsv, экспорт и т.д. — см. §8),
но пользователь его переприоритизировал двумя сообщениями:

1. **«Забей уже на комнату с экспонатом под лампой»** — restoration lab / shrouded
   exhibit больше НЕ трогаем. Смотрим: **атриум**, **планетарий** (срочно),
   **мониторы в комнате охраны** (смотрят не в ту сторону), **шкафы** под наш
   низкополигональный эмбиент.
2. **«Хочу именно модели GLB, возможно через Блендер»** — пропсы, собранные из
   примитивов прямо в `game/props/*.gd`, пользователя НЕ устраивают. Нужны
   настоящие `.glb`-файлы в едином low-poly стиле.
3. Опрос: ставить Blender — **да, поставить на ПК** (сделано). Что моделируем
   следующим пакетом — **всё сразу**: мониторы/техника охраны, вкрутить шкафы в
   комнаты, пропсы атриума, планетарий.

**Жёсткость проверки (главное правило, не смягчать):** любой объект не на своём
месте = сразу переделка (правка исходника → recapture → визуальная проверка).
Никаких «необычно, но валидно». Структурный тест PASS **не является**
доказательством. Доказательство — **только реально открытое изображение**.
Финальные решения по геометрии/положению — по native-кадрам (640×268) и
tile-мозаикам без ресайза, contact sheet — только первый фильтр.

**Не коммитить без явного разрешения пользователя.** Перед удалением временных
файлов — спросить.

---

## 1. Окружение

| что | значение |
| --- | --- |
| Корень проекта | `C:\Users\litvi\OneDrive\Документы\museum-liminal-v-0.9.4` |
| Godot | `C:\Users\litvi\OneDrive\Desktop\godot\Godot_v4.7-stable_win64_console.exe` (4.7.stable.official.5b4e0cb0f, D3D12, Forward+, Jolt) |
| Blender | `C:\Program Files\Blender Foundation\Blender 5.2\blender.exe` — **5.2.0 LTS, Python 3.13.13, НЕ в PATH** |
| Windows Python | `C:\Users\litvi\AppData\Local\Programs\Python\Python312\python.exe` (3.12.10, PIL 12.2.0) |
| winget | `C:\Users\litvi\AppData\Local\Microsoft\WindowsApps\winget.exe` v1.29.280 |
| MCP | сервер `opencode` живёт внутри редактора Godot, **регулярно падает** — просто подождать и повторить, не долбить |

Проверено headless-пробой (`tools/lowpoly/blender_probe.py`):
`io_scene_gltf2` включён по умолчанию, `bpy.ops.export_scene.gltf` работает,
.glb экспортируется. **GUI не нужен.** Форма запуска:

```
"C:\Program Files\Blender Foundation\Blender 5.2\blender.exe" -b --factory-startup --python tools\lowpoly\<script>.py
```

---

## 2. Как вообще смотреть картинки (НЕ ОЧЕВИДНО, читать обязательно)

`file_read_media` почти всегда обрезает вывод (видно 26–38% base64). Полный JSON
при этом сохраняется на диск сэндвича. Рабочий пайплайн, проверен 6 раз:

1. `mcpServer_opencode.runTool { toolName: "file_read_media", toolArguments: { path } }`
   → полный JSON автоматически ложится в
   `/data/tool-results/connections.mcpServer_opencode.runTool--<toolUseId>.json`
2. `connections.computer.terminal` (нужен `taskDescription`, <10 слов):
   ```
   python3 -c 'import json,base64,os;os.makedirs("/data/shots",exist_ok=True);d=json.load(open("<json>"));[open("/data/shots/x.jpg","wb").write(base64.b64decode(c["data"])) for c in d["content"] if c.get("type")=="image"];print(os.path.getsize("/data/shots/x.jpg"))'
   ```
3. `connections.computer.readFile { path: "/data/shots/x.jpg" }` — **только этот шаг
   реально показывает изображение.** Без него ты картинку НЕ видел.

Перед любым использованием `connections.computer.*` читай `modules/computer/AGENTS.md`
и `modules/computer/index.ts`.

---

## 3. Как снимать кадры

```
set "DECOR_CONFIG=res://decor_focus_office.json" && "C:\Users\litvi\OneDrive\Desktop\godot\Godot_v4.7-stable_win64_console.exe" --path . --script res://game/test_decor_capture.gd
```

* **Без `--headless`** (иначе нет рендера).
* **Кавычки вокруг всего присваивания обязательны.** `set X=v && cmd` кладёт в
  переменную пробел перед `&&` → Godot ругается `missing res://...json`.
* Дефолтный конфиг — `decor_shots.json` (132 канонических ракурса,
  out `res://shots/decor_audit/after`, tiles=2, settle=40, frames=6).
* Формат выхода: `<dir>/<name>.png`, `<dir>/view/<name>.jpg` (640×268),
  `<dir>/tiles/<name>_r<N>c<N>.jpg`.
* Сборка контактного листа:
  `python tools\make_focus_sheet.py <view_dir> <out.jpg> <cols> <name1> <name2> ...`
  (натуральная вставка без ресемплинга, подписи, нужен PIL).

---

## 4. СДЕЛАНО

### 4.1. Планетарий — 2 раунда правок, оба проверены глазами

Раунд 1 (`game/props/PlanetariumProps.gd`, 8 правок + `decor_shots.json` +
`FirstMuseumMap.gd:2617`): исправлены купол, ярусы сидений, проектор, опечатка
`rack.rotation.x` → `rail.rotation.x`, четыре ракурса планетария переведены с
`node_path` на ручные камеры.

Раунд 2: `SHROUD` → `Pal.tone(Pal.STONE, -0.52)`; ярлык экспоната перенесён на
`c + Vector3(-9.78, 1.95, 1.60)`; камеры проектора и будки перенацелены.

Подтверждено на открытом кадре (`sheet_v2.jpg` → `/data/shots/pl3.jpg`):
саван ✅, проектор ✅, будка оператора ✅.

**Осталось (см. §5):** ярлык `planetarium_sign` не виден, зелёная панель
EXIT пересвечена.

### 4.2. Low-poly GLB-тулчейн — работает, 4 модели в игре

Написан **`tools/lowpoly/glb.py`** — чистый stdlib-генератор glTF 2.0 binary.
Ни Blender, ни pip-пакетов не требует (PNG-атлас пишется вручную через `zlib`).
Именно поэтому его можно `import glb` изнутри Python'а Blender'а.

API (номера строк актуальны):

```
darkened(c, amount)          :96
lightened(c, amount)         :102
tone(c, amount)              :109
mixed(c, towards, amount)    :114
SWATCHES = {...}             :149   # порядок вставки = индекс в атласе
ATLAS_COLS = 8               :197
ATLAS_BLOCK = 16             :198
ATLAS_SIZE = 128             :199
swatch_index(name)           :202
uv_for(name) -> (u, v)       :211
atlas_png_bytes()            :228   # готовый PNG атласа, bytes
class Mesh:                  :327
    tri(a,b,c,swatch)                     :345
    quad(a,b,c,d,swatch)                  :355
    box(centre,size,swatch,faces=None,face_swatches=None,rot=None)  :360
    panel(centre,size,swatch,axis="pz",rot=None)                    :385
    cylinder(centre,radius,height,sides,swatch,...)                 :411
    taper(base_centre,bottom_xz,top_xz,height,swatch,...)           :446
    tri_count / vert_count / bounds()     :480 :484 :487
    check(require_floor=True)             :495
write_glb(path, mesh, wall_mounted=False) :542
```

`_BOX_FACES` — ключи `px/nx/py/ny/pz/nz`. `_rotate` использует порядок Эйлера
**YXZ** (как в Godot).

**`tools/lowpoly/build_props.py`** собирает 4 модели. Итог (раунд 2, проверено):

| модель | габарит W×H×D | tris | verts | байт | крепление |
| --- | --- | --- | --- | --- | --- |
| `lp_metal_locker` | 0.96 × 2.00 × 0.60 | 140 | 420 | 16 956 | пол |
| `lp_filing_cabinet` | 0.48 × 1.32 × 0.69 | 124 | 372 | 15 248 | пол |
| `lp_shelf_unit` | 1.00 × 1.90 × 0.42 | 178 | 534 | 21 052 | пол |
| `lp_key_cabinet` | 0.44 × 0.56 × 0.16 | 60 | 180 | 8 312 | стена |

Файлы в `models/lowpoly/` (по 4 файла на модель: `.glb`, `.glb.import`,
`_palette_atlas.png`, `.png.import`).

**`game/MapModels.gd` пропатчен:** добавлен `_resolve_path()` с трёхуровневым
поиском `MODEL_PATHS` → `res://models/<name>.glb` → `res://models/lowpoly/<name>.glb`;
`place()` и `has_model()` переведены на него; `lp_key_cabinet` добавлен в
`NON_BLOCKING`.

**`game/test_lowpoly_preview.gd`** — студийный превью-стенд, 8 ракурсов,
запуск: `"<godot>" --path . --script res://game/test_lowpoly_preview.gd`.

Два раунда визуальной правки значений цвета сделаны и **проверены на открытых
кадрах** (`/data/shots/lp1.jpg`, `/data/shots/lp2.jpg`). Все 4 модели признаны
годными.

### 4.3. Blender установлен

Через `winget install BlenderFoundation.Blender`. Лог `%TEMP%\blender_install.log`
заканчивается «Успешно установлено». Проба скриптинга пройдена (см. §1).

### 4.4. Комната охраны — диагноз поставлен по РЕАЛЬНО ОТКРЫТОМУ кадру

Снято `decor_focus_office.json` (7 ракурсов) → `shots/decor_audit/focus_office/`,
контактный лист открыт как `/data/shots/office1.jpg`.

**Найдено: на одном столе стоят ДВА монитора из двух разных пайплайнов с
конфликтующим масштабом.**

* Слева, тонкая плоская панель со светящимся экраном, x ≈ −25.6 — это
  импортированный `basic_pc_monitors`, `FirstMuseumMap.gd:2171`, scale 0.5, yaw 180.
* Справа, огромный коробчатый оливковый мёртвый ЭЛТ, x = −25.0 — это
  `OfficeProps.build_workstation_computer` (`:1247`), размещён в локальных
  `(0, 0, 0.50)` от `build_watcher_office` → мир ≈ `(−25, ·, −1.90)`.

Геометрия камеры для различения лево/право: `office_desk_close` стоит в
`(−25.3, 1.15, −0.95)`, смотрит в `(−25.4, 0.92, −1.95)` — практически строго
вдоль −Z без рыскания, значит право экрана = +X = менее отрицательный x.

Прочее: оливковый ЭЛТ по величине сопоставим с целым монитором настенного банка —
читается как холодильник, дефект масштаба/стиля. В `office_desk_rake_east`
импортный монитор показывает безликую тёмную заднюю плиту у переднего края стола.
В `office_desk_rake_west` оба экрана смотрят почти в лоб камере с запада, то есть
развёрнуты к −X, а не к креслу в `(−24.95, ·, −0.98)`.
Настенный банк мониторов, стена документов, кресло и бумаги — **в порядке**.

**Решение:** не подкручивать yaw. Удалить ОБА настольных монитора и заменить
одним связным комплектом, авторским в Blender: `lp_desk_monitor` + `lp_pc_tower`
+ `lp_keyboard`.

### 4.5. Пакет 2 (Blender) — `lp_desk_monitor`, `lp_pc_tower`, `lp_keyboard`

**`tools/lowpoly/blender_build.py` написан и работает.** Запуск:

```
"C:\Program Files\Blender Foundation\Blender 5.2\blender.exe" -b --factory-startup --python tools\lowpoly\blender_build.py
```

Скрипт импортирует `glb.py` (stdlib-only) и берёт оттуда палитру, атлас и
`uv_for()`, поэтому Blender-модели гарантированно одноцветны со шкафами
пакета 1. Класс `Part` — обёртка над `bmesh`: `box`, `taper`, `panel` плюс то,
ради чего всё затевалось: `bevel()` (фаска силуэта) и `inset()` (реальная
утопленная ниша под экран и под клавишное поле). На выходе проверки house
style: пол на y=0, центровка по X и Z, лимит тришек, ненулевой файл, ни одной
непомеченной грани.

Итог сборки (третий прогон, все bevel/inset реально сработали):

| модель | габарит W×H×D | tris | faces | байт |
| --- | --- | --- | --- | --- |
| `lp_desk_monitor` | 0.520 × 0.539 × 0.190 | 60 | 30 | 6 024 |
| `lp_pc_tower` | 0.190 × 0.426 × 0.460 | 72 | 36 | 6 860 |
| `lp_keyboard` | 0.440 × 0.024 × 0.150 | 48 | 24 | 5 176 |

`BUILD_OK 3 models`. В `models/lowpoly/` теперь 7 моделей = 28 файлов.
Импорт в Godot прошёл. `game/test_lowpoly_preview.gd` расширен с 8 до 13
ракурсов (`desk_front`, `desk_rake`, `desk_low`, `tower_detail`,
`keyboard_detail`) плюс стенд-ин стола: `DESK_TOP_Y 0.74`, `DESK_X 3.90`,
`DESK_Z 0.10`, две торцевые панели. Bounds в логе превью верные:
монитор и клавиатура `floor_y +0.7400`, башня `floor_y +0.0000`.

**Проверено на реально открытых кадрах** (`/data/shots/desk1.jpg`, затем
`/data/shots/desk2.jpg`):

* раунд 1 — все три модели белые, без единого цвета палитры. Причина найдена:
  `glb.uv_for()` возвращает UV в glTF-пространстве (V идёт ВНИЗ от левого
  верхнего угла), а Blender считает V ВВЕРХ и экспортёр сам пишет `1 - v`.
  Значения уезжали в пустую нижнюю половину атласа, и каждая грань брала
  пустоту. Лечится предварительным флипом `loop[uv].uv = (u, 1.0 - v)` в
  `Part.finish()` — он ровно компенсирует флип экспортёра;
* раунд 1 — кадр `tower_detail` был почти чёрный: камера упиралась в торцевую
  панель стенд-ин стола. Перенацелена на
  `[5.72, 0.66, -1.02] -> [4.98, 0.32, 0.14]`, fov 42;
* раунд 2 — цвета появились, `tower_detail` читается, фаски и ниши на месте.

**Ground truth палитры** (из `tools/lowpoly/glb.py`, чтобы больше не гадать):
`carcass` 0.152/0.175/0.190 · `carcass_dark` 0.104/0.120/0.130 ·
`carcass_side` 0.128/0.147/0.160 · `glass_dead` 0.020/0.024/0.028 (почти
чёрный) · `shadow` 0.035/0.041/0.048 · `glass_sheen` 0.100/0.131/0.150.
Вся палитра НАМЕРЕННО тёмная: игра освещена парой жёстких точечных
источников, средне-серый проп читается как светящийся.

**«Выглядит белёсым — виноват свет» НЕ ИСПОЛЬЗОВАТЬ как диагноз.** Эта
формулировка стояла здесь раньше и один раз уже породила ложный дефект (§4.6).
Проп судить ЧИСЛАМИ, а не впечатлением: корпус из палитры даёт
СИНЕДОМИНАНТНЫЙ пиксель (B > R, около `30,33,40`), эмиссивная плита
экрана — ЗЕЛЕНОДОМИНАНТНЫЙ и яркий (около `125,132,118`), бумага — ТЁПЛЫЙ
(R > G > B). Инструмент замера — `tools/lowpoly/probe_pixels.py`.

### 4.6. Комплект стола вкручен в карту и проверен (раунд 3, 2026-08-06)

* `FirstMuseumMap.gd:2160–2208` — вызов `basic_pc_monitors` удалён (причина
  записана комментарием на `:2167`), вместо него три `MuseumModels.place`:
  `lp_desk_monitor (-25.00, 0.74, -2.06) 1.0 180°`,
  `lp_keyboard (-25.04, 0.74, -1.70) 1.0 180°`,
  `lp_pc_tower (-24.14, 0.00, -1.80) 1.0 180°`.
* Эмиссивная плита `Workstation Screen` `(-25.00, 1.1105, -2.0500)`,
  `0.40 × 0.23 × 0.004`, `Color(0.070, 0.105, 0.090)`, energy 0.85,
  `rotation_degrees (-7, 0, 0)`; плюс `Workstation Monitor Led` и
  `Workstation Tower Led` (янтарный `Color(0.835, 0.604, 0.259)`).
* `OfficeProps.gd:1240–1281` — из `build_workstation_computer` вырезаны
  `Monitor Base/Neck/Case/Bezel/Screen` и `Monitor Power Led`; остались
  `Mouse Pad`, `Mouse`, `Tower Cable`; `tower_x/tower_z` сохранены только ради
  кабеля.
* **Группа `workstation_screen` не потеряна** — восстановлена на новой плите
  (`:2197`). Поиск по всему проекту (`findstr /s /n "workstation_screen" *.gd`)
  даёт ровно три вхождения: `:2191` комментарий, `:2197` добавление в группу,
  комментарий `OfficeProps.gd:1244`. **Потребителей у группы нет**,
  `MonitorWall.gd` от неё не зависит — ломать было нечего.

Лист в одну колонку из 7 офисных кадров (`sheet_office_lp1.jpg`, 640×1974)
собран, декодирован и **реально открыт**: дубликата монитора нет, комплект
читается как одна вещь, клавиатура со стороны кресла (+Z от монитора), башня
стоит на полу у правого торца стола.

**Ложный дефект D7 — закрыт измерением, а не глазом.** На открытом листе
`lp_*` показались белёсыми и едва не ушли в переделку. Опровергнуто по шагам:

1. `tools/lowpoly/inspect_glb.py` — материал пакета 1 и пакета 2 идентичен:
   `baseColorFactor [1,1,1,1]`, `baseColorTexture` привязана, атлас 128×128
   встроен в `.glb` (410 байт), `magFilter 9728` (NEAREST);
2. `tools/lowpoly/probe_pixels.py` по самому атласу: все сэмплы тёмные, а
   НЕиспользованные блоки залиты `9,10,12` — светлого пикселя в атласе нет
   вообще, поэтому и мип-бликование не могло бы осветлить проп;
3. превью-стенд при одинаковом свете: `locker_detail` 11–56,
   `lineup_front` 20–46 — пакеты 1 и 2 одинаково тёмные;
4. обратная проекция камеры `office_desk_close` показала: светлое пятно
   (100–150, зелёнодоминантное) — это ровно эмиссивная плита экрана, а корпус
   под ней даёт `30,33,40` (синедоминантный `carcass`), клавиатура — `20–30`.

Палитра в карте верна, чинить было нечего. Попутно выяснилось:
**`gltf/embedded_image_handling=1` (Extract Textures) в `.glb.import` — это норма**,
именно поэтому рядом с каждой моделью лежит извлечённый Godot'ом
`_palette_atlas.png`, и это НЕ признак поломки.

---

### 4.7. Атриум, ресепшен — раунд 5 (2026-08-06): два «дефекта» опровергнуты замером, добавлены клавиатуры и системник

Все 10 кадров `focus_atrium` открыты по-настоящему (лист в ОДНУ колонку, 640 px).
На глаз казалось, что (а) табличка «ЦЕНТР СДЕРЖИВАНИЯ» вылезла за плиту и
(б) столешница ресепшена пересвечена. Оба вердикта сняты замером по пикселям —
править было нечего:

1. **Табличка.** `atr_a3_core_close`: яркие пиксели (люма >190) в нижней
   половине кадра занимают x 232..407 — **175 px**. Камера (0, 1.55, 3.9),
   плита на z 2.40 → дистанция ≈1.5 м; fov 50 по вертикали, кадр 640×268 →
   ≈191 px/м. Значит надпись ≈**0.92 м вместе с обводкой 5 px** при коробке
   переноса 0.94 м и плите 1.00 м — внутри плиты. Комментарий в коде
   (`AtriumProps.gd:356-369`) верен: 36 px / `pixel_size` 0.0035 / `width` 0.94
   НЕ трогать. Крупной надпись выглядит только потому, что лист смотришь
   растянутым, а плита `PANEL` почти чёрная.
2. **Столешница.** `atr_c2_reception_top`, прямоугольник (120..520, 150..230)
   целиком по камню: min 7 · медиана 52 · p95 148 · **max 191** из 255.
   Клиппинга нет — это градиент блика, а не пересвет. `COUNTER_STONE` с
   roughness 0.10 оставлен как есть.

Что действительно было не так: на столе стояли **два монитора и ни одной
клавиатуры**, при том что `lp_keyboard` и `lp_pc_tower` лежали в
`models/lowpoly/` без места. Обе модели вкручены в `build_reception_desk`:

- модели авторены «начало координат на опорной плоскости, лицом в −Z»;
  у ресепшена клерк стоит со стороны локального −Z, поэтому рыскание здесь
  **0**, а не 180, как на столе в кабинете охраны;
- клавиатура — `(side * 0.90 − 0.04, 1.10, −0.50)`: тот же отступ 0.36 м к
  оператору, что и в кабинете (монитор z −2.06, клавиатура z −1.70). 0.15 м
  глубины модели не задевают ни ногу монитора (z −0.24), ни задний край
  столешницы (z −0.62);
- системник — `(1.30, 0.00, −0.95)`, на полу в нише клерка: правее
  возвратного крыла (кончается на x −1.29) и позади тумбы (z −0.62);
- у обоих `Models.place` оставлен процедурный запасной ящик на случай
  отсутствующего `.glb` — ровно как у монитора выше по функции.

Проверка: в `decor_focus_atrium.json` добавлен 11-й ракурс
**`atr_c5_reception_clerk`** (pos −5.17, 1.70, 8.86 → look_at −4.25, 1.05,
10.20, fov 54) — единственный кадр, с которого рабочее место видно со стороны
клерка. Пересъёмка 11 кадров, лист `sheet_n2.jpg` (640×846, c5+c2+c1) открыт
глазами: **обе клавиатуры лежат перед своими мониторами**, столешница
читается. Системник с этого ракурса закрыт тумбой — глазами НЕ подтверждён,
стоит только по коду. Кому достанется следующий заход: либо ракурс ниже и
правее, либо признать проп невидимым и снять.

---

## 5. ОТКРЫТЫЕ ДЕФЕКТЫ

| # | где | что | как чинить |
| --- | --- | --- | --- |
| D1 | планетарий | ярлык экспоната в мире `(-9.78, 1.95, -39.40)` не виден в кадре `planetarium_sign` — пустая стена | проверить `visibility_range_end 22.0` у `_add_label` (`FirstMuseumMap.gd:243`) против дистанции камеры и fov 40 |
| D2 | планетарий | пересвеченная зелёная панель EXIT/Emergency над проёмом | найти источник, приглушить |
| D6 | пайплайн | контактный лист шире 1024 px транспорт УЖИМАЕТ (1280→1024) — это уже не native, судить по нему о геометрии нельзя | собирать лист в ОДНУ колонку: `make_focus_sheet.py <view_dir> <out.jpg> 1 <names...>` → ширина ровно 640 px |
| D10 | атриум | `atr_c4_cable_ceiling` (2.0, 1.70, 4.5 → 8.5, 3.25, 9.0, fov 60) читается пустым: ровный серый потолок, крошечный зелёный EXIT и один тёмный ящик — кабель-трассы `build_cable_runs` в кадр не попадают | сверить цель ракурса с реальными координатами трасс; если трасс в этом углу нет — либо ракурс не туда, либо вешать нечего |
| D11 | атриум | `atr_a1_from_door` (13.2 м до ядра, fov 62): зал читается пустым и туманным, ядро мелкое и низкоконтрастное, напольная разметка не видна | это композиция и туман, а не баг; чинить осознанно, отдельным заходом |

**Закрыто в раунде 3 (2026-08-06):** D3 и D4 — оба старых монитора удалены,
`lp_*` комплект стоит в карте, вердикт снят с native-листа в одну колонку (§4.6).
D5 — `desk_key` понижен до `energy 1.5 / range 7.5`
(`test_lowpoly_preview.gd:232-233`), стенд переснят, 13 кадров, пропы больше
не пересвечены (`locker_detail` 11–56).
D7 («белёсые `lp_*` в карте») — ложная тревога, закрыта измерением (§4.6).

**D8 и D9 (монитор-волл, панели 1 / 3 / 5) — ПОЧИНЕНЫ одной правкой**
в `test_map_verification.gd` (была строка 1617). Дефект оказался в ТЕСТЕ, арт
был ни при чём. Корень: тест телепортировал игрока-`CharacterBody3D` к панели,
сразу ставил `look_at` и ТОЛЬКО ПОТОМ давал 4 кадра осесть. Тело всё это
время падало под гравитацией и расталкивалось из геометрии, так что к моменту
чтения прицел устаревал: 0.45 м просадки на дистанции 1.5 м — это 16.7° при
допуске `WATCH_PANEL_ANGLE_DEG = 9.0`. Поэтому же ответ «плавал» от веса сцены:
проверка меряла тайминг оседания, а не прицел. Новый порядок: 8 кадров
осесть → 4 кадра целиться ЗАНОВО КАЖДЫЙ кадр → финальный `look_at` → чтение
`watched_feed()`. Результат: все 6 панелей зелёные, суита — `✅ ALL CHECKS PASSED`,
70 строк, 0 красных. **Урок на будущее:** прежде чем винить свою правку,
проверь, что тест вообще меряет то, что заявляет.

---

## 6. ЧТО ДЕЛАТЬ ДАЛЬШЕ (по порядку) — ИСТОРИЯ, СПИСОК ВЫПОЛНЕН

> **УСТАРЕЛО 2026-08-09.** Все пункты этого раздела выполнены: `blender_build.py`
> написан и с тех пор дополнен (`Part.prism`, `Part.swing`), моделей в
> `models/lowpoly/` уже 17, ядро сдерживания собрано моделями и закоммичено
> (`c282788`). Актуальный порядок работ — в `NEXT_CHAT_PROMPT.md`, раздел «Текущая
> работа», и в `AGENT_PLAN.md`, раздел «Текущая работа — партия low-poly моделей
> в Blender». Ниже оставлено как справка по технике авторинга: пункт 1 подробно
> описывает идиом Blender-скрипта, и это по-прежнему верно.

> **ПУНКТЫ 1–4 НИЖЕ УЖЕ ВЫПОЛНЕНЫ (2026-08-06)** — подробности в §4.5 и §4.6:
> `blender_build.py` написан, три модели собраны и импортированы, превью
> расширено до 13 ракурсов, UV-баг починен, свет стенда понижен,
> оба старых монитора снесены, `lp_*` комплект стоит в карте и проверен
> на реально открытом native-листе.
>
> **ПУНКТ 5 ТОЖЕ ВЫПОЛНЕН (2026-08-06, раунд 4).** 11 экземпляров моделей
> пакета 1 добавлены в архив и склад и приняты по реально открытым кадрам.
> Без места остался только настенный `lp_key_cabinet` — офис для него закрыт.
>
> Замерено заранее, чтобы не ломать тесты при пункте 5:
> • `StorageProps.hazard_cabinet` — габарит 1.00 × 2.05 × 0.60 (`StorageProps.gd:298`),
>   почти точно `lp_metal_locker` (0.96 × 2.00 × 0.60) — самый дешёвый кандидат,
>   НО у него задокументированы контракты: коллизия 1.00 × 2.05 × 0.58 и
>   24–28 узлов, плюс нарратив (шевроны, треугольник, пломба);
> • `Stabilization Locker` (`:1770`) — это цельный короб 1.35 × 2.5 × 3.0,
>   шкаф 1:1 в него НЕ ложится, а его положение асёртит `_verify_office_fixtures`
>   (задняя грань ровно в 2.5 см от стены);
> • хайд-споты (`:2070`, `:2072`) — требуют ПОЛОЙ оболочки: их проверяет
>   `test_map_verification._verify_hide_spots()` — капсула игрока внутри плюс луч
>   зрения снаружи, так что просто подменить их монолитной моделью нельзя.
> Вывод: начинать с тех мест, где шкаф ДОБАВЛЯЕТСЯ в комнату, а не подменяет
> протестированный объект (картотека архива, стены склада).

1. **`tools/lowpoly/blender_build.py`** — авторинг в Blender.
   * `sys.path.insert(0, os.path.dirname(__file__))`, затем `import glb`.
     `glb.py` — stdlib-only, поэтому импортируется под Python 3.13.13 Blender'а.
     Берём оттуда `SWATCHES`, `uv_for()`, `atlas_png_bytes()` — палитра будет
     буква в букву та же, что у четырёх готовых шкафов.
   * Строить через `bmesh`, каждой грани `poly.use_smooth = False`, один материал.
   * Атлас: записать `glb.atlas_png_bytes()` во временный PNG, завести
     `bpy.data.images.load`, интерполяция `Closest`, экспортировать встроенным.
   * Экспорт: `bpy.ops.export_scene.gltf(filepath=..., export_format="GLB", export_apply=True, export_yup=True)`.
   * Стиль тот же: 1 юнит = 1 м, начало координат на полу по центру X/Z,
     вперёд = −Z, без нормалмапов/металла/эмиссии.
   * Пакет 2: `lp_desk_monitor`, `lp_pc_tower`, `lp_keyboard`
     (далее по желанию `lp_radio_set`, `lp_cctv_switcher`).
2. Импорт в Godot: `"<godot>" --headless --path . --import`.
   **Перед реимпортом чистить кэш атласа:**
   `del /q models\lowpoly\*.import models\lowpoly\*_palette_atlas.png`
   — иначе Godot не заметит смену палитры.
3. Расширить список ракурсов в `game/test_lowpoly_preview.gd`, прогнать,
   собрать лист, **декодировать и реально открыть**.
4. ✅ **ВЫПОЛНЕНО (§4.6).** Оба настольных монитора удалены, `lp_*` комплект
   стоит в карте, группа `workstation_screen` перенесена на эмиссивную плиту,
   потребителей у группы в проекте нет, кадры пересняты и открыты.
5. ✅ **ВЫПОЛНЕНО (2026-08-06, раунд 4).** 11 моделей поставлены ТОЛЬКО как
   дополнение — ни один протестированный объект не подменён. Правка внутри
   `_add_model_archive`, между строкой `vents` и `# --- Entrance Zone`.

   | Комната | Модель | Позиция (world) | Yaw |
   |---|---|---|---|
   | Архив | `lp_filing_cabinet` ×3 | x −26.60 / −26.08 / −25.56, z −16.31 | 180 |
   | Архив | `lp_shelf_unit` | (−16.20, 0, −16.44) | 180 |
   | Архив | `lp_metal_locker` | (−34.35, 0, −12.00) | −90 |
   | Склад | `lp_metal_locker` ×3 | x −32.00 / −31.00 / −30.00, z 16.35 | 0 |
   | Склад | `lp_shelf_unit` ×2 | x −21.00 / −19.90, z 16.44 | 0 |
   | Склад | `lp_filing_cabinet` | (−18.50, 0, 7.695) | 180 |

   Тест после правки: `✅ ALL CHECKS PASSED`, дельта ровно **+11 мешей / +11 тел /
   +11 шейпов** — один меш и один коллайдер на модель, лишних узлов нет.
   Приёмка глазами: `decor_focus_lp_pack1.json` (10 кадров), `…1b.json`
   (пересъёмка бракованного `arch_shelf_corner`), `…1c.json` (4 складских
   ракурса вдоль чистой полосы). Все листы декодированы и РЕАЛЬНО открыты.
   **НЕ трогать `OfficeProps.build_key_cabinet`** — в нём нарратив (пустой крючок +
   упавший брелок), закрытый застеклённый ящик его уничтожит.
   Хвост пункта: `lp_key_cabinet` (настенный) места пока не получил.
6. **Атриум** — частично ВЫПОЛНЕНО (раунд 5, §4.7): все 10 ракурсов
   `focus_atrium` открыты, две мнимые поломки (табличка ядра, блик на
   столешнице) сняты замером — **не открывать их заново**; ресепшен
   одет в `.glb` (2×`lp_keyboard` + `lp_pc_tower`), тест зелёный.
   Остаток: D10 (кабель-трассы не в кадре) и D11 (читаемость зала от двери)
   в §5, плюс старый разбор `review_2026-08-06/atrium.jpg`, если там осталось
   что-то ещё (вызов строителя — `FirstMuseumMap.gd:4071`).
7. Планетарий D1 и D2.
8. После любой правки карты — перепрогнать `test_map_verification.gd`
   (счётчик мешей уедет с **9947** — последнее измеренное число, раунд 5,
   см. §11; старое 9960 там же помечено как недостоверное).
9. **Прочитать `decor_shots.json` целиком** — обязательный по ТЗ файл ориентации,
   пока только грепался.

---

## 7. Грабли (не наступать повторно)

* **СКЛАД НЕ ПУСТ, И ГРЕПОМ ПО `StorageProps.` ЭТОГО НЕ ВИДНО.**
  `GameManager._build_devices()` строит В РАНТАЙМЕ четыре верстака
  6.8 × 1.1 × 1.25 (`GameManager.gd:1809–1811`: `bench_x` −30.75 / −19.25,
  `bench_z` 9.2 / 15.0). Занято: x −34.15..−27.35 и −22.65..−15.85,
  z 8.575..9.825 и 14.375..15.625, верх **y = 1.10**.
  Свободны ровно три полосы: z 7.35..8.575, z 9.825..14.375, z 15.625..16.65.
  Всё это записано в `FirstMuseumMap.gd:1991–1997` — читать ПЕРЕД расстановкой.
  Историю уже проходили: `:2792` «Every one of them stood inside a bench» —
  четыре стойки, швабра и ведро были вырезаны по этой же причине.
* **Проверяй позицию КАМЕРЫ по таблице занятости, а не только цель съёмки.**
  `arch_shelf_corner` вернулся сплошным размытым пятном: камера
  (−18.40, 1.60, −14.30) стояла ВНУТРИ rolling stacks B (x −21.2..−17.0,
  z −16.25..−13.25) — кадр не доказал ничего. На складе та же ошибка повторилась
  иначе: верстак высотой 1.10 м срезал низ у всех четырёх ракурсов, и это
  выглядело как «модели висят». Лечится съёмкой ВДОЛЬ чистой полосы, а не поперёк.
* **Посчитай, где будет линия среза, прежде чем звать дефект.** Верхняя дальняя
  кромка верстака даёт из `store_locker_bank` 151 px при наблюдаемых 155, из
  `store_shelves` — 145 при 150, из `store_south_cabinet` — 104 при 113.
  Совпадение расчёта с картинкой доказало, что модели стоят на полу: пересечений
  с верстаками нет ни у одной (зазоры 0.425 / 0.605 / 0.535 м).
* **cmd:** `set X=v && cmd2` кладёт в переменную хвостовой пробел.
  Правильно: `set "X=v" && cmd2`.
* **findstr:** несколько шаблонов — в ОДНОЙ строке в кавычках:
  `findstr /r "^11[5-9]: ^12[0-9]:"`. Второй кавыченный аргумент трактуется как
  имя файла (`FINDSTR: Cannot open ...`).
* `dir` / `findstr` без совпадений возвращают `isError: true` — это не сбой.
* **Читай возвращаемый диф КАЖДОГО `file_edit`.** Один раз так поймана
  собственная опечатка `rack.rotation.x` вместо `rail.rotation.x`.
* **Не угадывай палитру.** `CASING` (люма ≈0.12) темнее `CONCRETE` (≈0.19) —
  предположение об обратном было опровергнуто чтением `Palette.gd`.
* Правка может породить новый дефект: перенос ярлыка в раунде 1 наложил его на
  вывеску комнаты. Всегда переснимать и смотреть.
* **Бинарники через MCP не пишутся** — любые бинарные ассеты генерирует скрипт,
  запущенный на Windows через `command_run`.
* `mesh.create_convex_shape(true, false)` в `MapModels.gd` — `simplify`
  намеренно `false` (замеры: `наблюдатель` 780.9→183.0 мс и т.д.).
* Не удалять записи из `PLACEHOLDER_MODELS` в `MapModels.gd`, не заменив файл.
* Игнорировать устаревшую копию `.claude\worktrees\sad-chatelet-405028\game\MapModels.gd`.

* **bmesh НЕ пересчитывает нормали граней сам** — ни при создании грани, ни
  после `bevel`/`inset`. Без `bm.normal_update()` выборка граней по нормали
  молча возвращает пустоту, `bevel` и `inset` становятся no-op, и на экспорт
  уходит обычная коробка без единого ошибочного сообщения. Единственный
  симптом — подозрительно малое число граней в отчёте. Поэтому в
  `Part.facing()` есть параметр `expect=` и жёсткий `SystemExit` при
  несовпадении, а в лог печатаются строки `BEVEL ... out N` и
  `INSET ... sides N`. Если там нули — фаски не сделались.
* **UV из `glb.uv_for()` нельзя отдавать Blender'у как есть** — нужен флип
  `1.0 - v`, иначе модель экспортируется белой (подробно в §4.5).
* **Картинка шире 1024 px приходит АГЕНТУ УЖАТОЙ.** 1280×1122 превращается
  в 1024×898. Поэтому обзорный лист годится только как первый фильтр, а
  решения по геометрии — только по листу в одну колонку (640 px) или по
  `tiles/*_rNcN.jpg`.
* **`file_read_media` кладёт полный JSON в `/data/tool-results/` ТОЛЬКО когда
  вывод обрезался.** Если картинка уместилась целиком — файла на диске нет
  и декодировать нечего. Вместе с предыдущим пунктом это даёт узкое окно:
  картинка должна быть **не шире 1024 px** (иначе ужмётся) и при этом
  **достаточно тяжёлой**, чтобы вывод обрезался и JSON лёг на диск.
  Рабочий рецепт: лист в ОДНУ колонку из 6–10 кадров — ширина остаётся
  640 px, а высота и вес гарантируют обрезку. Один кадр 640×268 и даже
  лист 640×1122 из трёх кадров проходят целиком и НЕ кэшируются —
  проверено дважды.
* **MCP периодически отваливается** (`Failed to connect to MCP server`).
  Лечится паузой в 30–60 секунд и повтором. Не долбить подряд.

### Принятые проектные решения (и почему)

* **PIL отвергнут для `glb.py`** — атлас пишется через `zlib`+`struct`, чтобы у
  модуля был ноль сторонних зависимостей. Это же делает безопасным `import glb`
  изнутри Blender.
* **Вершинные цвета отвергнуты** — `COLOR_0` + `vertex_color_use_as_albedo`
  опирается на недокументированное поведение импортёра Godot; атлас детерминирован.
* **`doubleSided: false`** — отсечение включено намеренно, чтобы вывернутая грань
  давала дырку и ловилась на ревью.

---

## 8. Остаток исходного ТЗ (пока отложен пользователем)

* Открыть `shots/decor_audit/after/final_detail_review/final_detail_review_1..8.jpg`
  и 3 lab-мозаики (`lab_exhibit_sheet_tiles.jpg`, `lab_covered_west_tiles.jpg`,
  `lab_covered_east_tiles.jpg`). **Лаборатория пользователем снята с повестки.**
* Полный позиционный проход по всем 132 ракурсам.
* `shots/decor_audit/audit.tsv` — ровно 132 строки, колонки
  `name, status, issue, game_or_capture, planned_fix, after_status`.
* Расширить `game/test_decor_layout.gd`: banners NE/EN/ES, симметрия
  Exit/Emergency, зазоры дверных проёмов.
* Прогнать `run_tests.cmd`, `check_materials.gd`, `check_localization.py`,
  `git diff --check`.
* Обновить `AGENT_PLAN.md`: итоги аудита, актуальный mesh count, ночная яркость,
  статус decor-тестов и экспорта.
* Новый Windows-экспорт и smoke-test именно нового `build/windows/MuseumLiminal.exe`.

---

## 9. Временные файлы (спросить пользователя перед удалением)

### A/B-стенд на `git worktree` — главный инструмент раунда 3

Путь: `C:\Users\litvi\AppData\Local\Temp\museum-head-check` (~1.2 ГБ).
**ОСТАВЛЕН ПО РЕШЕНИЮ ПОЛЬЗОВАТЕЛЯ (2026-08-06)** — не удалять без нового спроса.

**Состояние на конец раунда 4: ЧИСТЫЙ HEAD `eb21a23`, отслеживаемых правок ноль**
(проверка: `git -C <стенд> status --short --untracked-files=no` печатает пустоту).
После бисекции там оставались наши `game/MapModels.gd`, `game/FirstMuseumMap.gd`
и `game/props/OfficeProps.gd` — то есть стенд был **ГИБРИДОМ, а не эталоном**, и именно
такой гибрид однажды дал ложно-зелёный прогон (9939 мешей — модели просто не
загрузились). Возвращено через `git -C <стенд> checkout -- <файлы>`.

**Правило: после КАЖДОГО A/B возвращать стенд в HEAD тем же checkout, а нужные
файлы копировать поверх ТОЛЬКО на время конкретного замера.**
Проверяй состав стенда ПЕРЕД прогоном, а не после: число мешей — единственный
дешёвый детектор того, что `.glb` вообще подхватились.

`.godot` и `models/lowpoly/` в стенде не отслеживаются и оставлены специально:
без них Godot переимпортирует проект, а HEAD-версия `_resolve_path()` всё равно не
видит `models/lowpoly/` — это норма для эталона.

Отвечает за один прогон на вопрос «это наша регрессия или она была всегда».
Именно он снял обвинение с low-poly комплекта и вывел на настоящий дефект в тесте.

```
git worktree add --detach "%TEMP%\museum-head-check" HEAD
robocopy ".godot" "%TEMP%\museum-head-check\.godot" /E /NFL /NDL /NJH /NJS /MT:16
robocopy "models\lowpoly" "%TEMP%\museum-head-check\models\lowpoly" /E /NFL /NDL /NJH
copy /Y "game\<file>.gd" "%TEMP%\museum-head-check\game\<file>.gd"
"<godot>" --path "%TEMP%\museum-head-check" --script res://game/test_map_verification.gd
```

Правила, выплаченные кровью:

- Копирование `.godot` (1190.2 МБ / 1411 файлов) убирает полный реимпорт.
  Без него стенд поднимается минутами.
- Подкладывать файлы **ПО ОДНОМУ** и мерить после каждого. Иначе получите
  ЛОЖНЫЙ ЗЕЛЁНЫЙ: без `MapModels.gd` HEAD-овский `_resolve_path()` не видит
  `models/lowpoly/`, модели просто не грузятся, и тест зеленеет по причине
  «проверять нечего». Сверяйтесь с числом мешей: пришло ли то, что клали.
- **`file_edit` заперт в корне проекта** и в стенд писать НЕ будет
  (`Access denied - path outside allowed directories`). Править там только через
  PowerShell: `[System.IO.File]::ReadAllText/WriteAllText` с `UTF8Encoding($false)`,
  иначе в `.gd` прилетит BOM.
- Удаление, когда разрешат: `git worktree remove --force <путь>`, затем `git worktree prune`.
- Чужой worktree `.claude/worktrees/sad-chatelet-405028` @ `22a4110` — НЕ ТРОГАТЬ.


Из исходного ТЗ: `game/test_wall_probe.gd`, `run_decor_probes.cmd`,
`run_decor_probes.ps1`, `run_decor_focus_round2.ps1`, `run_decor_focus_round3.ps1`,
`run_decor_focus_round4.ps1`, `run_decor_final.ps1`, `decor_camera_probes.json`,
`decor_focus_round2.json`, `decor_focus_round3.json`, `decor_focus_round4.json`,
`tools/make_decor_probe_sheets.py`, `tools/make_focus_round2_sheets.py`,
`tools/make_focus_round3_sheets.py`, `tools/make_focus_round3_detail_sheets.py`,
`tools/make_focus_round4_detail_sheets.py`,
`tools/make_remaining_after_detail_sheets.py`, `tools/make_final_contact_review.py`,
`tools/make_final_decor_detail_review.py`.

Добавлено на этом этапе: `decor_focus_planetarium.json`, `decor_focus_office.json`,
`tools/make_review_montage.py`, `tools/make_focus_sheet.py`,
`game/test_lowpoly_preview.gd`, `tools/lowpoly/blender_probe.py`.

Добавлено в раунде 4 (пункт 5): `decor_focus_lp_pack1.json`,
`decor_focus_lp_pack1b.json`, `decor_focus_lp_pack1c.json` и каталог
`shots/decor_audit/focus_lp_pack1/`. Изменённые файлы раундов 3–4 сверх ранее
перечисленных: `game/test_map_verification.gd` (починка прицела монитор-волла,
+17/−3) и `game/FirstMuseumMap.gd` (комплект стола + пункт 5).

**Постоянные, не удалять:** `tools/lowpoly/glb.py`, `tools/lowpoly/build_props.py`,
`tools/lowpoly/blender_build.py`, `models/lowpoly/*`, правки в `game/MapModels.gd`,
`models/README.md`, а также два диагноста из раунда 3:
`tools/lowpoly/inspect_glb.py` и `tools/lowpoly/probe_pixels.py`.

`inspect_glb.py` печатает материалы, сэмплеры, UV-диапазоны и блоки атласа
прямо из `.glb` — именно он закрывает вопрос «привязана ли текстура» без
пересъёмки. `probe_pixels.py` печатает средний RGB по сетке кадра или по
указанным прямоугольникам — именно он опроверг ложный D7. Оба read-only,
оба запускаются Windows-питоном, `probe_pixels.py` требует PIL.

---

## 10. Якоря в коде

### `game/FirstMuseumMap.gd`
```
:243   _add_label(...)  Label3D, font_size 32, pixel_size 0.006, BILLBOARD_ENABLED,
                        visibility_range_end 22.0, outline_size 4
:266   _material(color, transparent, emission_energy, metallic)
:368   _add_room(...)
:622   Камера 09 - Planetarium (-9.2, 3.0, -34.4) -> (0, 1.2, -41)
:1324  Planetarium Ceiling Light (0,0,-41) Color(0.45,0.52,0.9) 0.45 11.0
:1680  Office Partition West x -26.35 w 1.30 / East x -23.45 w 1.70, z -2.46, h 2.50
:1708  Office Records Credenza (-25.0, 0.575, 5.55) 2.60x1.15x0.52
:1713  build_watcher_office(parent, (-25,0,-2.4), 0.0, "", "")
:1729  MonitorWall.gd
:1740  Alarm Terminal Pedestal (-28.2, 0.55, -2.38)
:1761  Stabilization Locker (-33.95, 1.25, 4.45) + Locker Shelf %s
:2017  StorageProps.hazard_cabinet(root, (-34.0, 0, 7.35), 0.0, ...)
:2070  HideSpot Storage Hide Locker (-26.6, 0.0, 7.55) 180.0
:2072  HideSpot Office Hide Locker (-33.85, 0.0, 0.5) -90.0
:2160  _add_model_archive
:2171  basic_pc_monitors (-25.6, 0.74, -1.95) 0.5, 180.0   <-- УДАЛИТЬ
:2176  tactical_flashlight (-24.35, 0.767, -1.78) 0.25, 25.0
:2185  wooden_bookcases_with_books (-34.48, 0, -5.16) 1.0, 90.0
:2601  _add_planetarium_details, c := Vector3(0,0,-41)
:2616  PlanetariumProps.build_all
:2617  ярлык экспоната c + Vector3(-9.78, 1.95, 1.60)
:2636  ArchiveProps.build_restoration_lab
:2710  Wing Banners (+-1.7)
:4039  _add_room(... "Planetarium" ...)
:4055  _add_door_frame(map_root, (0,0,-33), "x")
:4071  AtriumProps.build_atrium(map_root, Vector3.ZERO)
:4078  _add_planetarium_details(map_root)
```
Комната охраны 20×14 в `(-25,0,0)`; восточная стена `-15.35`, северная `z -6.65`,
западная `x -34.65`; столешница `z -2.26..-1.54`, края стола `x -26.2..-23.8`.

### `game/props/OfficeProps.gd`
```
:434  MONITOR_W 0.52, MONITOR_H 0.40, MONITOR_PITCH_X 0.58, MONITOR_PITCH_Y 0.46,
      MONITOR_ROW0_Y 1.28
:461  build_monitor_bank(parent, origin, columns=3, rows=2, yaw_deg=0.0,
      with_collision=true) -- локальный z=0 это плоскость стены, +Z в комнату,
      экраны на z 0.139; группа "monitor_bank"
:850  build_key_cabinet(parent, origin, empty_hook=4, yaw_deg=0.0, label_text="")
      -- дверца открыта на -105 градусов, 3 рейки x 4 брелка, Dropped Key Fob,
      БЕЗ КОЛЛАЙДЕРА. НЕ ЗАМЕНЯТЬ на lp_key_cabinet.
:929  build_document_wall(parent, origin, columns=5, ...)
:1193 build_watcher_office(parent, origin, yaw_deg, cabinet_label, roster_label)
      -- origin НА ПОЛУ у основания стены мониторов, bbox 4.50 x 2.38 x 1.79
:1247 build_workstation_computer(parent, origin, yaw_deg=0.0)
      -- Monitor Base/Neck/Case/Bezel/Screen :1280-1294, Monitor Power Led :1297,
         группа "workstation_screen"                      <-- МОНИТОР УДАЛИТЬ
DESK_TOP_Y 0.74
```

### `game/props/PlanetariumProps.gd` (состояние после раунда 2)
```
Центр комнаты мир (0,0,-41), 20x16, WALL_HEIGHT 3.4, проём (0,0,-33) DOOR_GAP 1.8
build_all: build_dome(ZERO), build_projector(ZERO),
           build_seating_bank((0,0,-4.85)), build_operator_booth((7.72,0,4.85))
DOME_SPRING_Y 2.30, DOME_APEX_Y 3.28, CEILING_CLEAR_Y 3.32
Сидения: tread 1.50, riser 0.45, width 15.0, rows 3, bench_width 14.40, front 2.25
Будка: half_x 1.85, half_z 2.70, wall_t 0.16; мир x 5.87..9.57 / z -38.85..-33.45
SHROUD = tone(STONE, -0.52)
Отступы: табы; внутри for-цикла 2 таба, продолжения 3 таба
```

### `game/MapModels.gd` (пропатчен)
```
@tool, без class_name, static var _cache: Dictionary
MODEL_PATHS = {"camera": "res://models/camera.fbx", "security_camera": ...}
NON_BLOCKING = [..., "lp_key_cabinet"]
TRIMESH_COLLISION = ["portal_arch"]
PLACEHOLDER_MODELS = ["falling_cube","broken_clock","frozen_drop","portal_arch",
                      "superheavy_sphere"]
_resolve_path(name): MODEL_PATHS -> res://models/<name>.glb
                     -> res://models/lowpoly/<name>.glb -> ""
place(parent, model_name, world_position, scale_factor=1.0, rotation_y_deg=0.0,
      pitch_x_deg=0.0)  -- Эйлер YXZ, вперёд -Z, значит ОТРИЦАТЕЛЬНЫЙ pitch
      смотрит вниз
has_model(name) -> not _resolve_path(name).is_empty()
```

### Ракурсы
```
decor_focus_planetarium.json (7, out res://shots/decor_audit/focus_planetarium)
  overview_planetarium  [0,1.65,-35]      -> [0,1.6,-42]      fov 72
  planetarium_dome      [-1.8,1.65,-37.4] -> [0.4,3.15,-41.4] fov 74
  planetarium_projector [2.6,1.55,-38.6]  -> [0,1.35,-41.0]   fov 48
  planetarium_seating   [-5.6,1.75,-41.0] -> [0.2,0.95,-45.8] fov 60
  planetarium_booth     [3.4,1.62,-36.6]  -> [6.6,1.35,-36.0] fov 52
  planetarium_sign      [-6.2,1.75,-39.6] -> [-9.6,1.95,-39.4] fov 40   (D1)
  planetarium_door      [0,1.7,-36.4]     -> [0,2.3,-33.1]    fov 50

decor_focus_office.json (7, out res://shots/decor_audit/focus_office)
  office_overview       [-25.0,1.70,1.20] -> [-25.0,1.45,-2.40] fov 62
  office_bank           [-25.0,1.55,-0.30]-> [-25.0,1.55,-2.40] fov 58
  office_seated         [-25.0,1.25,-0.60]-> [-25.2,0.95,-2.10] fov 55
  office_desk_close     [-25.3,1.15,-0.95]-> [-25.4,0.92,-1.95] fov 42
  office_desk_rake_east [-23.2,1.30,-1.10]-> [-25.6,0.92,-1.95] fov 46
  office_desk_rake_west [-27.4,1.30,-1.05]-> [-25.4,0.92,-1.95] fov 46
  office_desk_top       [-25.2,2.35,-0.70]-> [-25.3,0.80,-1.95] fov 52

decor_focus_lp_pack1.json (10, out res://shots/decor_audit/focus_lp_pack1)
  arch_overview       [-25,2.20,-8.60]     -> [-25,1.15,-15.60]   fov 66
  arch_filing_run     [-26.10,1.45,-13.60] -> [-26.10,0.70,-16.25] fov 50
  arch_filing_rake    [-23.90,1.35,-14.60] -> [-26.30,0.66,-16.30] fov 48
  arch_shelf_corner   [-18.40,1.60,-14.30] -> [-16.30,0.95,-16.40] fov 50  БРАК: камера внутри stacks B
  arch_west_locker    [-31.80,1.55,-11.60] -> [-34.30,1.00,-12.00] fov 50
  store_overview      [-25,2.20,8.60]      -> [-25,1.15,15.60]    fov 66
  store_locker_bank   [-31.00,1.60,13.40]  -> [-31.00,1.05,16.30] fov 52  низ срезан верстаком
  store_locker_rake   [-28.20,1.50,14.20]  -> [-31.60,1.00,16.30] fov 50  низ срезан верстаком
  store_shelves       [-20.40,1.60,13.60]  -> [-20.40,0.95,16.40] fov 52  низ срезан верстаком
  store_south_cabinet [-18.50,1.40,10.30]  -> [-18.50,0.66,7.75]  fov 48  низ срезан верстаком

decor_focus_lp_pack1b.json (1, тот же out) — пересъёмка брака, ПРИНЯТО
  arch_shelf_corner   [-16.30,1.55,-12.60] -> [-16.25,1.00,-16.30] fov 45

decor_focus_lp_pack1c.json (4, тот же out) — вдоль чистой полосы, ПРИНЯТО
  store_locker_bank2   [-26.90,1.60,15.85] -> [-31.30,0.80,16.30] fov 58
  store_shelves2       [-23.30,1.55,15.85] -> [-20.30,0.85,16.40] fov 55
  store_south_cabinet2 [-21.50,1.45,8.35]  -> [-18.50,0.75,7.80]  fov 52
  store_north_wall     [-25.00,2.70,10.60] -> [-25.00,1.30,16.40] fov 70
```

---

## 11. Актуальные метрики

`test_map_verification.gd` → **✅ ALL CHECKS PASSED** (прогон 2026-08-06, раунд 3,
после починки прицела монитор-волла): 70 строк вывода, **0 красных**.

**Low-poly библиотека:** 7 моделей в `models/lowpoly/` (28 файлов с учётом
`.import` и атласов), суммарно **682 tris** — меньше, чем шесть гладких
`CylinderMesh` (128 tris штука).

**Актуальные числа (прогон 2026-08-06, раунд 4, ПОСЛЕ пункта 5, рабочее дерево):**
MeshInstance3D **9960** · StaticBody3D **564** · CollisionShape3D **565**.
Дельта пункта 5 к раунду 3 — ровно **+11 / +11 / +11**.

**Раунд 5 (2026-08-06, после §4.7, рабочее дерево):** MeshInstance3D **9947** ·
StaticBody3D **564** · CollisionShape3D **565** · Label3D **133**,
`✅ ALL CHECKS PASSED`. Мешей МЕНЬШЕ записанных выше 9960 на 13 — при том, что
раунд только добавлял пропы. Значит, число 9960 снято не с того состояния
дерева, что сейчас; верить надо этому прогону. Тела и шейпы не изменились,
потому что GLB-пропы ставятся без коллизий.

Остальные счётчики сняты прогоном раунда 3 (ДО пункта 5) и не перемерялись:
Label3D 133 · Node3D 11785 · Marker3D 13 · Light3D 50 · Camera3D 2 ·
CharacterBody3D 1 · WorldEnvironment 1.

Для сравнения — чистый HEAD `eb21a23`, тот же тест через `git worktree`:
MeshInstance3D 9963 · StaticBody3D 555 · CollisionShape3D 556 · Node3D 11797 ·
Light3D 50. Дельта раунда 3: **−14 мешей, −2 тела, −2 шейпа, свет без
изменений** — ровно снятие двух процедурных мониторов против трёх `lp_*`
моделей, плиты экрана и двух светодиодов.

**Осторожно:** прежнее число 9967 в этом файле было неверным ДАЖЕ для HEAD
(там 9963). Не доверяйте ему в старых заметках — сверяйтесь только с прогоном.

Три красные проверки прицела монитор-волла (D8 и D9) **ПОЧИНЕНЫ** 2026-08-06 —
правка порядка операций в `test_map_verification.gd`, разбор в §5. Теперь зелены
все шесть панелей: `✓ Monitor wall: each of the 6 panels names its own post when
looked at, the dead one stays silent`. Числа выше сняты уже после этой правки.

Три из четырёх моделей пакета 1 в карте СТОЯТ — 11 экземпляров, §6 п. 5,
приняты по реально открытым кадрам. Без места остался только настенный
`lp_key_cabinet`.
