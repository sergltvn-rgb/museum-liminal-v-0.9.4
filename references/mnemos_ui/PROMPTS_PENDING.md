# МНЕМОС — ожидающие генерации

Использовать общий префикс стиля из `AGENT_PLAN.md`, блок 10 / раздел промптов.

## 10 — Экран завершения ночной смены

```text
End-of-shift report screen printed like a museum ledger: a header with shift number and duration, a table of resolved events with inventory numbers and verdicts, and a summary line at the bottom. One row of the summary contradicts the others - the count of registered closures is higher than the count of resolved events - and that single discrepancy line is the only thing rendered in pale violet-white #C7B7D9. Beige paper-like panel over graphite background. Everything else is calm and clerical. 16:9
```

Целевой файл: `10_end_shift_report.jpeg`.

## 12 — Искажение интерфейса аномалией

```text
The observer HUD while an anomaly distorts it. The layout is completely intact and every critical value stays legible - the distortion sits ON TOP of the interface, not inside its layout. Effects: ghosted previous states of the timer text trailing behind by a few pixels, one interface layer offset horizontally from the rest, a service label rendered mirrored, and a line reading "OBSERVERS: 2" where it should read 1. Pale violet-white #C7B7D9 only on the ghosted text. Restrained, uncanny, not a glitch-art poster. 16:9
```

Целевой файл: `12_anomaly_distortion.jpeg`.
