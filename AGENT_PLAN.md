# Museum Liminal — рабочий документ проекта (для агента)

Единая точка входа для следующего агента (Godot 4.7). Сверено с реальным кодом и git
2026-08-02. Читать целиком до первой правки.

> **Правило документа:** здесь пишется только то, что проверено командой или чтением кода.
> Непроверенное помечается явно. Код — источник истины.

Зеркало этого файла ведётся в Notion; при расхождении верить репозиторию.

---

## 1. Что это за проект

Хоррор от первого лица про три ночные смены в ПЕРВОМ МУЗЕЕ — музее аномальной физики.
Игрок работает НАБЛЮДАТЕЛЕМ: находит разрыв, диагностирует его с терминала, подтверждает
по CCTV, приносит нужный прибор со склада и уходит в карманное измерение, чтобы закрыть
аномалию изнутри. Измерений десять. Со второй ночи по музею ходит КУРАТОР. RU + EN.

Порядок величин: ~11 300 строк игрового GDScript + ~1 200 строк тестов, 398 ключей
локализации, 42 сцены, 312 скриптов, 22 GLB, 20 WAV, 5 headless-наборов тестов.

**Проект состоит из двух почти независимых слоёв, и агенты обычно видят только один:**

1. **Геймплейный слой** — ночи, аномалии, приборы, Куратор, 10 карманных измерений, CCTV,
   меню, локализация, аудио. Написан, покрыт тестами, описан в `README.md`, `HANDOFF.md`,
   `RIFT_TRIALS.md`.
2. **Визуально-архитектурный слой** — процедурная генерация здания, материалы, экстерьер,
   освещение. Именно им идёт активная работа, и именно он описан в разделах 4–8.

Правки в визуальном слое почти всегда безопасны для геймплейного, но **обратное неверно**:
правка геометрии может сломать навмеш, проходимость дверей и тесты карты.

---

## 2. Пути и окружение

- Корень: `C:\Users\litvi\OneDrive\Документы\museum-liminal-v-0.9.4`
- Godot: `C:\Users\litvi\OneDrive\Desktop\godot\Godot_v4.7-stable_win64_console.exe`
  (4.7.stable, D3D12, RTX 3070)
- Физика Jolt, рендер Forward+, главная сцена `scenes/FirstMuseumMap.tscn`
- Сейвы: `C:\Users\litvi\AppData\Roaming\Godot\app_userdata\Museum liminal\` →
  `museum_progress.cfg` (флаг `story/drive_seen`), `museum_save.cfg`, `museum_settings.cfg`
- Игнорировать при обходе файлов: `.claude/`, `.qoder/`, `.godot/`, `models/` (1.67 ГБ)

---

## 3. Инструменты агента (MCP)

Сервер называется `mcpServer_opencode`. Вызов строго такой:

```js
connections.mcpServer_opencode.runTool({
  toolName: "file_read_text",
  toolArguments: { path: "project.godot" },
})
```

То есть `{toolName, toolArguments}` — **не** `args`. Рабочая папка сервера = корень проекта,
все пути в `file_*` относительные.

Группы инструментов: `file_*` (чтение, запись, правка, поиск, дерево, перемещение, удаление),
`godot_*` (версия, редактор, отладочный вывод, UID), `scene_*`, `project_run` / `project_stop` /
`project_get_info`, `tests_run` / `tests_run_single` / `tests_audit`, `command_run`.

**MCP живёт внутри Godot-редактора и регулярно отваливается** с «Failed to connect to MCP
server». Это норма: подождать и повторить. Если лежит долго — попросить пользователя открыть
Godot.

### Быстрая проверка состояния

| Что | Команда |
|---|---|
| Материалы (14 наборов) | `cmd /c ""<godot>" --headless --path . --script res://game/tools/check_materials.gd" 2>&1 \| findstr /v /c:"registered mcp"` |
| Скриншоты 6 ракурсов (НЕ headless) | `cmd /c ""<godot>" --path . --script res://game/test_shot.gd" 2>&1 \| findstr /v /c:"registered mcp"` |
| Просмотр кадра | `file_read_media {"path": "shots/view/<name>.jpg"}` — только до ~30 КБ |
| Полные тесты | `tests_run` (склонен к таймауту) либо `run_tests.cmd` через `command_run` |
| Локализация | `python tools/check_localization.py` |

---

## 4. Структура проекта

### Сцены

Геометрии карты в репозитории нет. `scenes/FirstMuseumMap.tscn` — 38 строк: корень и девять
пустых узлов-менеджеров. Весь музей строит `FirstMuseumMap.build_map()` в рантайме при каждом
запуске. Правьте геометрию в коде, а не мышью в сцене.

- `scenes/FirstMuseumMap.tscn` — основная карта
- `scenes/TutorialPrologue.tscn` — интерактивный пролог из семи шагов
- `scenes/trials/*.tscn` — 10 сцен-дескрипторов карманных измерений

### Визуально-архитектурный слой (`game/props/`, 17 файлов)

- `MaterialLib.gd` — `@tool class_name MaterialLib`, 14 текстурных наборов, API
  `get_material(pack_name, tint, scale, triplanar)`. Наборы: `travertine, mosaic, quartzite,
  concrete, steel, painted_metal, corroded_metal, window_grime, rust, plastic_dry,
  plastic_worn, dirt, asphalt, wood`
- Экстерьер: `BuildingShell.gd` (оболочка/крыша), `FacadeProps.gd` (портик),
  `ExteriorProps.gd` (деревья, фонари, машины, горы, река), `GroundsProps.gd` (территория
  и парадный двор)
- Вестибюль: `LobbyProps.gd` — стиль одобрен пользователем
- Интерьерные крылья: `AtriumProps`, `OfficeProps`, `ArchiveProps`, `StorageProps`,
  `MassProps`, `TimeProps`, `CorridorProps`, `LightProps`, `PlanetariumProps`,
  `GravityProps`, `SpaceProps`

Итого 16 файлов пропсов + `MaterialLib.gd`. Во всех 16 есть `_pack_for` и врезка
pack-маршрутизации в единую точку материала.

### Геймплейный слой (`game/`)

- `GameManager.gd` (1540 строк) — цикл ночей, состояния, приборы, HUD, экран провала
- `GameplayEnhancements.gd` — эффекты аномалий, CCTV-задачи, синхронизация Куратора
- `CuratorMonster.gd` — Куратор: `CharacterBody3D` со своим `_physics_process`
- `ExhibitPuzzleController.gd` — 12 экспонатов, выбор носителя разрыва, привязка камеры
- `RiftTrialManager.gd` (1070 строк) + `game/trials/` — механики всех десяти измерений
- `SecurityCameraTablet.gd` — планшет на 11 камер и процедурная мини-карта
- `MenuManager.gd`, `SettingsPanel.gd`, `SettingsManager.gd` — меню и настройки
- `PlayerController.gd`, `PlayerScaleController.gd` — движение и масштаб игрока
- `AudioManager.gd` (20 WAV, музыки нет), `CRTOverlay.gd` + `shaders/`, `UITheme.gd`,
  `Compass.gd`, `InventoryBar.gd`, `HideSpot.gd`, `RiftGate.gd`, `MonitorWall.gd`,
  `TerminalFrame.gd`, `Cutscene.gd`
- `Loc.gd` — безопасная замена `tr(KEY) % args`; `InputBootstrap.gd` — действия в рантайме
- `FeedbackManager.gd` + `notion_worker/` — приём отзывов, в сборку не попадает

### Карта: 11 комнат

`Entrance Zone` (0,25) · `Central Atrium` (0,0) · `Watcher Office` (−25,0) ·
`Equipment Storage` (−25,12) · `Archive` (−25,−12) · `Restoration Lab` (−25,22) ·
`Gravity Wing A` (28,0) · `Mass Wing D` (52,0) · `Time Wing B` (0,−24) ·
`Space Wing C` (24,−24) · `Planetarium` (0,−41)

### Тесты — их 13, а не 3

В `run_tests.cmd` и CI входят пять: `test_project_integration`, `test_map_verification`,
`test_incident_catalog`, `test_full_game_cycle`, `test_blocker_regressions`.
Вручную: `test_shell_intrusion` (интрузии оболочки в комнаты), `test_shot` (грузит всю сцену,
ловит parse error), `test_visual_capture`, `test_lobby_capture`, `test_exterior_capture`,
`test_facade_capture`, `test_drive_capture`, `test_full_walkthrough_capture`,
`test_10_runs_audit`.

### Инструменты и ассеты

- `game/tools/check_materials.gd` — эталон «Проверено карт: 59, наборов: 14, ошибок: 0»
- `game/tools/make_wood.gd`, `make_roughness.gd` — генераторы текстур
- `game/AgentShots.gd` + `game/test_shot.gd` + `agent_shots.json` — 6 ракурсов, JPEG до ~31 КБ
- `tools/scan.ps1`, `tools/extract_textures.ps1` — PowerShell только через
  `cmd /c "powershell -NoProfile -ExecutionPolicy Bypass -File tools\<name>.ps1"`
- `textures/` — 14 наборов (Poliigon даёт готовый `_Roughness`; ambientCG требует конверсии
  GLOSS→ROUGHNESS; `WoodProcedural/`)
- `models/` — 1.67 ГБ, 22 GLB + FBX + 42 исходных ZIP; кандидат на диету
- `localization/game.csv` (398 ключей, `keys,en,ru`) + скомпилированные
  `game.en.translation` / `game.ru.translation`

> Правка `game.csv` без пересборки `.translation` ни на что не влияет — игра грузит
> скомпилированные файлы, они перечислены в `project.godot`.

---

## 5. Плагины (`addons/`)

| Плагин | Может ли агент использовать | Вердикт |
|---|---|---|
| PlantGenerator | **ДА** — чистый GDScript, кодовый API (`PlantGenerator` RefCounted: axiom/rules/steps; `Plant3D extends MeshInstance3D`). Гонять из `--script`, сохранять меш через `ResourceSaver` в `.tres` | Главный кандидат: реальные деревья вместо цилиндро-сфер |
| SimpleGrassTextured | Частично — `grass.gd` это `@tool MultiMeshInstance3D`, инстанцируется из кода | Опционально: живая трава на газонах |
| Asset Placer | НЕТ — док редактора, нужна мышь | Только для пользователя в GUI |
| MaterialLayers | Практически нет — настраивается в редакторе | Низкий приоритет |
| TerraBrush | НЕТ — GDExtension dll не грузится (ошибка при `--import`) | Не трогать, из git исключён |
| godot_ai | Уже используется — через него подключён MCP | Единственный включённый в `project.godot` |

Для использования Plant3D/SimpleGrass из кода включать плагин не обязательно (preload по пути).

---

## 6. Что уже сделано

1. **Вестибюль, экстерьер, оболочка** — стили одобрены; интрузии устранены
   (`[OK] No shell geometry reaches into any room`)
2. **Территория и парадный двор** — принято пользователем
3. **Инструментарий агента** — AgentShots / test_shot / check_materials / scan
4. **Катсцена приезда возвращена** (сброшен `drive_seen`)
5. **Текстурная база** — 14 наборов, «Проверено карт: 59, наборов: 14, ошибок: 0»
6. **Pack-маршрутизация во всех 16 файлах пропсов + карта.** Сознательно НЕ текстурятся:
   стекло, светящиеся элементы, вода, бумага, экраны, двусторонние поверхности купола
   планетария
7. **Git приведён в порядок** — см. раздел 9

---

## 7. План работ (по приоритету)

1. **Освещение вестибюля и атриума.** По кадрам `lobby_reception.jpg` и `atrium_centre.jpg`
   слишком темно, новые текстуры не читаются. Перебалансировать
   `LobbyProps.build_lobby_lighting :466` и свет атриума.
2. **PlantGenerator → деревья.** Сгенерировать 2–3 вида (дуб, берёза, сосна), экспортировать
   меши в `.tres`, заменить процедурные `build_oak` / `build_birch` / `build_pine`
   в `ExteriorProps.gd`, **сохранив сигнатуры билдеров**.
3. **Двор.** Заменить кустарные 6 фонарей (x ±4.5, z 39.5/45/50.5), 4 дерева
   (±25, z 39.5/50.5) и коробочные машины (z 59) на `ExteriorProps.build_lamp_post`,
   деревья и `build_parked_car`.
4. **`FirstMuseumMap._cylinder` — пробросить `pack`** (колонны, трубы, светильники).
   Подтверждено 2026-08-02: сигнатура на строке 77 —
   `(parent, node_name, cylinder_position, radius, height, color, horizontal,
   emission_energy, with_collision)`, параметра `pack` нет. Вызовов ~30, добавлять
   с параметром по умолчанию.
5. **Кнопка пересмотра катсцены** в `MenuManager._build_main_page()` рядом
   с `MENU_RESET_PROGRESS` (строка 602); сброс `story/drive_seen`; ключи
   в `localization/game.csv` (en+ru) с пересборкой `.translation`; затем
   `python tools/check_localization.py`. Подтверждено 2026-08-02: в `MenuManager.gd` нет ни
   одного упоминания `drive_seen` — не сделано.
6. **Траектория катсцены** — проверить `_drive_track` / `_drive_shots` на пересечение
   с `Court Gate` (z 54.2, x ±4.7) и `Fountain Collision` (0, 0.40, 49.5, r 3.06).
7. **Регрессы** — полный `tests_run`; `test_shell_intrusion.gd` при правках оболочки;
   `check_materials.gd` при правках материалов.
8. **Вес репозитория** — `models/` 1.67 ГБ + `textures/`; кандидаты на удаление или
   даунскейл: `DirtWindowStains005_ALPHAMASKED_2K.png` (16.8 МБ), `_BUMP16` / `_DISP16`,
   отработанные `_GLOSS` / `_REFL` / `_DISP`.

### Долги геймплейного слоя (не из этой очереди, но их спросят)

Подробности в `HANDOFF.md` и `IMPROVEMENT_PLAN.md`: нет ни одного музыкального трека;
нет системы субтитров; готовой сборки не существует (`--export-release` не выполнялся ни
разу); нет мид-сейва; Куратор бинарен (замер / бег по прямой); ночь не встаёт на паузу внутри
карманного измерения; автозагрузка `_mcp_game_helper` указывает на `addons/godot_ai`, который
вырезан из экспорта — первый же экспорт может упасть на старте.

---

## 8. Технические правила и ловушки

- `tests_run` без аргументов склонен к таймауту MCP. Быстрая проверка правок:
  `check_materials.gd` + прогон `test_shot.gd`
- `file_read_media` принимает только `{"path"}`, файлы до ~30 КБ; PNG таймаутят
- `command_run` — это **cmd, не PowerShell**. PowerShell только через `.ps1` файл:
  `cmd /c "powershell -NoProfile -ExecutionPolicy Bypass -File tools\<name>.ps1"`.
  Однострочники через `-Command` с запятыми и скобками ломаются на экранировании —
  проверено, не тратьте на это попытки
- Capture нельзя запускать с `--headless`
- `--import` ругается на terrabrush dll — норма, игнорировать
- `class_name` в голом `--script` НЕ регистрируется → всюду `preload`
- Pack игнорируется при `transparent` или `emission > 0` — сознательно
- `BoxMesh` разворачивает UV 0..1 на грань → обязателен трипланар с `uv1_scale` от мировых
  координат
- ambientCG отдаёт GLOSS → конвертер `make_roughness.gd`; Poliigon даёт готовый `_Roughness`
- `Label3D` не билбордить; `Object.tr()` недоступен из static →
  `TranslationServer.translate(key)`
- Тени off при `size.length() < 0.65`; компланарные плиты дают z-fighting — смещать
  на миллиметры
- `file_write` НЕ создаёт каталоги → сначала `file_create_directory`
- Навигация настроена впритык: `agent_radius` 0.45 и `cell_size` 0.15 дают ровно 0.9 м
  навмеша в дверном проёме. При 0.5 / 0.2 останется 0.6 м и Куратор начнёт застревать.
  **Не менять эти числа и не сужать дверные проёмы**

---

## 9. Git и GitHub

Репозиторий: <https://github.com/sergltvn-rgb/museum-liminal-v-0.9.4>

**Рабочая ветка — `wip/audit-2026-07-26`.** Она опережает `main` на 69 коммитов и не отстаёт
ни на один: вся актуальная работа живёт в ней, а `main` — старое состояние.

Что было не так и исправлено 2026-08-02: весь визуальный слой (14 текстурных наборов,
`MaterialLib`, `BuildingShell`, `ExteriorProps`, `FacadeProps`, `GroundsProps`, `LobbyProps`,
инструменты агента, capture-тесты) лежал вне git как untracked, а рядом копились мусорные
артефакты — скриншоты прогонов, временные скрипты, каталог `.qoder/`, неработающий terrabrush.

Правила:

- Коммитить в `wip/audit-2026-07-26`, в `main` напрямую не писать
- Скриншоты (`shots/`, `*_captures/`) и временные скрипты (`tools/tmp_*`) в git не попадают —
  они в `.gitignore`. Это рабочие артефакты, а не исходники
- Перед коммитом прогонять `check_materials.gd`, а при правках геометрии — ещё
  и `test_shell_intrusion.gd`
- Секреты живут только в `.env`, который в `.gitignore`. Никогда не вписывать ключи
  в исходники

---

## 10. Правила приёмки

- Отчитываться по каждому этапу отдельно, со скриншотами: `test_shot.gd` →
  `shots/view/*.jpg` → `file_read_media`
- Ничего не считать готовым без визуального подтверждения пользователем
- Модели качественные, переиспользование разрешено, процедурные текстуры равноправны
  с фото из `models/`

---

## 11. Ключевые сигнатуры

```gdscript
MaterialLib.get_material(pack_name, tint := Color.WHITE, scale := 0.0, triplanar := true) -> StandardMaterial3D
```

Приём pack-маршрутизации — шаблон во всех файлах пропсов:

```gdscript
const MatLib := preload("res://game/props/MaterialLib.gd")

static func _pack_for(color: Color) -> String:
    if color.is_equal_approx(STEEL): return "steel"
    return ""

# в точке материала, сразу после проверки кэша:
if not transparent and emission <= 0.0:
    var pack := _pack_for(color)
    if not pack.is_empty():
        var photo := MatLib.get_material(pack, color)
        _materials[key] = photo
        return photo
```

Якоря `_material`: `AtriumProps :853` · `OfficeProps :277` · `ArchiveProps :282` ·
`StorageProps :713` · `MassProps :885` · `TimeProps :618` · `CorridorProps :697` ·
`LightProps :824` · `PlanetariumProps :577` (кэш `_mats`) · `GravityProps._mat :~922` ·
`SpaceProps._mat :~110`

Прочее:

- `MapModels.place(parent, name, world_position, scale_factor, rotation_y_deg, pitch_x_deg)`
- Спавн игрока: (0, 0.05, 46), камера y 1.65, fov 72
- Навигация: `agent_radius` 0.45, `cell_size` 0.15

---

## 12. Куда смотреть дальше

| Файл | О чём |
|---|---|
| `README.md` | геймплей целиком: цикл ночей, приборы, измерения, управление, сборка |
| `HANDOFF.md` | честное состояние работ и известные расхождения |
| `IMPROVEMENT_PLAN.md` | дорожная карта геймплея |
| `RIFT_TRIALS.md` | механики десяти карманных измерений |
| `MODELS_GUIDE.md` | слоты моделей и правила коллизий |
| `CHANGELOG.md` | история версий |
| `game/AGENT_TOOLS.md` | инструментарий агента подробно |

---

## 13. Дорожная карта (задана пользователем 2026-08-02)

Порядок блоков — приоритет сверху вниз. Блоки 1 и 2 закрыты, начинать с блока 3.
Правило прежнее: ничего не считать готовым без визуального подтверждения пользователем.

### Блок 1 — катсцена заезда и туман · ЗАКРЫТО

Заезд можно пересмотреть: флаг `story/drive_replay` + `_clear_seen()` в `FirstMuseumMap.gd`,
кнопка `MENU_REWATCH_DRIVE` в главном меню. Туман разведён по профилям в
`_apply_fog_profile()` (~`FirstMuseumMap.gd:1157`): blackout 0.025/0.022, заезд 0.016/0.038,
день 0.0065. Профиль перечитывается из `SettingsManager._apply_quality()`.

### Блок 2 — деревья · ЗАКРЫТО (принято «более менее»)

Пайплайн: `tools/bake_trees.gd` (запекатель, `extends SceneTree`) → `models/trees/*.tres`
(4 дуба, 3 сосны, 3 берёзы) → `game/props/TreeLib.gd` → делегаты в `ExteriorProps`.
Запуск: `"<godot>" --headless --path . --script res://tools/bake_trees.gd`.

**Главная ловушка, стоившая двух пустых итераций:** поле `desired_width`, которое отдаёт
аддон PlantGenerator, почти не затухает вглубь дерева — у сосны каждая ветка возвращалась
почти во всю толщину ствола. Любой порог, привязанный к нему, бесполезен. Ширина теперь
считается своим способом: `base_width * pow(DEPTH_TAPER, depth)`, где `depth` — глубина
вложенности скобок L-системы. Облиствение включается по `depth >= recipe.leafy_depth`.
Сосне вдобавок понадобилась структурная правка правила: `Y → F[+L][-L][&L][^L]FY`
(старое `F[+L][-L]L` обрывалось после одного сегмента).

Текущий бюджет: дуб 7404 тр., сосна 3234, берёза 2850; вся библиотека 47 868 тр.
Если просядет FPS — крутить `LEAVES_PER_TIP` (9) и `LEAVES_PER_TWIG` (3).

**Хвост блока:** массивы деревьев ещё не переведены на `TreeLib.build_field()` —
`GroundsProps._forest :528` (пояса 54/48/26 деревьев), `Avenues :552`,
`FirstMuseumMap._add_drive_set` (26 + 24 дерева). Точки вызова: `GroundsProps.gd :521/:523/:525`,
`FirstMuseumMap.gd :3052/:3054/:3056`. Отдельно решить судьбу двух ручных берёз
`FirstMuseumMap.gd :3010–3011`.

### Блок 3 — двор, вид назад и налево, парковка

Самая заметная для игрока зона: это первое, что он видит, выйдя из машины.

- Оформить двор перед входом: 6 фонарей (x ±4.5, z 39.5 / 45 / 50.5), 4 дерева
  (x ±25, z 39.5 / 50.5), припаркованные машины на z 59.
- **Не задевать коллизии:** `Court Gate` (z 54.2, x ±4.7) и `Fountain Collision`
  (0, 0.40, 49.5, радиус 3.06). После любой правки геометрии двора — `test_shell_intrusion.gd`
  и `test_map_verification.gd`, иначе можно молча сломать навмеш.
- Вид назад и налево от входа сейчас пустой — там нужна своя композиция, а не повтор
  фасадной. Смотреть кадры `ext2_forecourt_corner` и `ext8_from_car_park`.
- Нормальная парковка вместо нынешней заглушки: `Museum Parking Lot` (42.4, −0.03, 46.5),
  размер 21.2 × 0.07 × 17.0, керб (42.4, 0.03, 38.2), 5 разметочных линий `x = 36 + i*3`
  на z 41.8, две машины (37.5, 0.005, 42.6) и (46.5, 0.005, 42.6). Нужны бордюры, знаки,
  столбики, разметка получше и больше машин.

### Блок 4 — текстуры по объектам, постепенно

Не «залить всё разом», а объект за объектом, с показом кадра после каждого.
`MaterialLib` сейчас знает 14 наборов: `travertine, mosaic, quartzite, concrete, steel,
painted_metal, corroded_metal, window_grime, rust, plastic_dry, plastic_worn, dirt, asphalt,
wood`. Шаблон подключения набора к пропсу — в разделе 11, якоря `_material` там же.

Ловушки: pack игнорируется, если материал `transparent` или `emission > 0`; `BoxMesh`
требует трипланар; ambientCG отдаёт GLOSS — прогонять через `make_roughness.gd`, у Poliigon
уже готовый `_Roughness`. Эталон приёмки: `Проверено карт: 59, наборов: 14, ошибок: 0`.

### Блок 5 — оптимизация

Сначала замеры, потом правки. Мерить FPS и draw calls в трёх точках: подъезд по дороге,
атриум, вестибюль. Дальше по результатам:

- окклюдеры — сейчас в проекте нет ни одного `OccluderInstance3D`;
- `MultiMesh` — используется только для неба в `SpaceProps :448–498`, деревья и мелочь двора
  просятся туда же;
- сжатие 2K-нормалей, чистка `models/` (каталог весит 1.67 ГБ, один
  `DirtWindowStains005_ALPHAMASKED_2K.png` — 16.8 МБ);
- дальности видимости уже расставлены, якоря: `AtriumProps :111` (115), `CorridorProps :94` (70),
  `LobbyProps :55` (44), `SpaceProps :72` (115), `ExteriorProps :198`, `LightProps :818`.

### Блок 6 — название музея и текст

- **Название.** Рабочее «ПЕРВЫЙ МУЗЕЙ / Museum Liminal» пользователя не устраивает: нужно
  оригинальное, пригодное для витрины Steam, хорошо звучащее на слух в обоих языках
  и свободное как имя. Сначала предложить список вариантов и утвердить, только потом менять
  в коде — название сидит в `localization/game.csv`, `project.godot`, заголовках меню
  и в пути сейвов `%APPDATA%\Godot\app_userdata\Museum liminal\` (смена пути обнулит
  прогресс, если не предусмотреть перенос).
- **Упростить перевод.** В каталоге 474 ключа; многие формулировки тяжелы для чтения
  с экрана. Задача — короче и живее, без потери смысла.
- **Дополнить перевод** там, где строки захардкожены мимо каталога.
- **Реплики под озвучку.** Пользователь планирует озвучивать их сам, поэтому фразы должны
  быть произносимыми вслух: короткие, с естественным дыханием, без канцелярита
  и без нечитаемых аббревиатур.
- Порядок работы: правка `localization/game.csv` → пересборка
  `"<godot>" --headless --import --path .` → проверка `python tools\check_localization.py`
  (эталон: `474 keys in catalogue, 474 used in code, locales: en, ru / OK`).
  **Правка csv без пересборки `.translation` ни на что не влияет** — частая потеря времени.

### Блок 7 — доделать катсцену заезда по моделям

Сам заезд работает (30 с, 5 шотов, `_drive_track` — 6 сегментов, ключи `STORY_DRIVE_01..05`),
но наполнение кадра слабое. Смотреть модели вдоль трассы, машину игрока, придорожную мелочь,
мост на x 150, остановку на x 75. Захват: `test_drive_capture.gd` → `drive_captures/` →
`python tools\tmp_shrink.py drive_captures`.

### Блок 8 — ревизия всех десяти измерений (требование пользователя)

Каждое карманное измерение нужно пересмотреть и довести до релизного уровня. Прямая речь
пользователя: «у некоторых механики отвратные», часть придётся переделать, а не починить.
То, что все десять проходят `test_full_game_cycle`, говорит только о том, что испытание
запускается и корректно прерывается. **Это не проверка того, интересно ли в нём играть.**

Список для ревизии (ид из `RiftTrialManager`): `gravity_surge`, `temporal_drift`, `radiation_bloom`,
`void_rift`, `echo_chamber`, `glass_bridge`, `mirror_maze`, `yellow_halls`, `scrap_run`, `ascent`.
Механики описаны в `RIFT_TRIALS.md`, код — `game/RiftTrialManager.gd` (~1070 строк).

По каждому измерению пройти один и тот же чек-лист и записать вердикт:

1. **Сыграть самому**, а не читать код. Записать, что игрок делает руками в течение всего
   испытания, посекундно.
2. **Глагол испытания.** Одно действие, ради которого оно существует. Если его нет —
   измерение переделывается целиком.
3. **Не повторяется ли сосед.** Два испытания с одним глаголом — повод одно из них слить
   или перепридумать.
4. **Читаемость правил без текста.** Игрок должен понять задачу по сцене за 10–15 секунд.
5. **Цена ошибки.** Провал должен быть понятным и быстро отыгрываемым; перезапуск без
   длинной прелюдии.
6. **Длительность.** Замерить фактическое время прохождения и сверить с остальными:
   разброс больше чем вдвое — проблема темпа ночи.
7. **Звук и свет** — есть ли у измерения своё звучание и своя палитра, или это серый ящик.
8. **Вердикт:** оставить как есть / довести / переделать механику / вырезать. Лучше восемь
   сильных измерений, чем десять, из которых два раздражают.

Работать по одному измерению за заход, с показом пользователю. После каждой правки —
`test_full_game_cycle` и `test_incident_catalog`: они ловят рассыпавшийся диспетчер и расхождения
каталога инцидентов.

### Блок 9 — смена визуального курса: пиксельная картинка

Пользователь хочет уйти от погони за реализмом к намеренно грубой, пиксельной картинке —
ориентиры названы самим: Lethal Company, R.E.P.O., Buckshot Roulette. Причина трезвая:
такого вида дешевле достичь силами одного человека, и он не проигрывает сравнению с AAA,
потому что в это сравнение не вступает.

Важно: проект уже заявлен как `MAP_STYLE "PS1 Horror"`, то есть это не разворот на 180°,
а доведение заявленного стиля до конца.

Порядок работы (от самого дешёвого к дорогому):

1. **Рендер в низком разрешении с растягиванием по ближайшему соседу.** Главный рычаг всего стиля
   и одновременно самая большая экономия кадра — то есть блок 9 работает на блок 5.
   Смотреть `rendering/scaling_3d/*` в `project.godot`. Начать с масштаба порядка 0.5–0.6
   и подобрать по кадрам. Сделать настройкой в `SettingsManager`, а не жёстко в коде.
2. **Фильтрация текстур — nearest.** Без этого низкое разрешение даёт не пиксели, а кашу.
3. **Убрать то, что противоречит стилю и стоит дорого:** SSR, SSIL, часть SSAO, агрессивный
   glow. Это всё в `_add_world_env()` (`FirstMuseumMap.gd` ~:1012). Объёмный туман оставить —
   он несёт атмосферу и был специально настроен в блоке 1.
4. **Цветовая дисциплина.** Ограниченная палитра и резкие тени читаются как стиль;
   полутона и мыло — как бедность. Цвета уже собраны в шапках пропсов — править там.
5. **Текстуры под новый курс.** Это меняет блок 4: вместо погони за 2K PBR — мелкие
   текстуры (256–512) без сглаживания. Заодно решается вопрос веса `models/` (1.67 ГБ).
6. **Проверка на читаемость.** Пикселизация бьёт по мелкому шрифту и по картинке CCTV.
   Интерфейс и планшет смотреть отдельно — возможно, UI надо оставить в полном разрешении
   поверх пиксельного 3D.

Решение принимать по кадрам до и после, на одной и той же точке обзора. **Пока курс не утверждён,
не вкладываться в дорогие фототекстуры по блоку 4** — их придётся выбросить.

#### Уточнение по референсам (пользователь прислал три скриншота)

**Главное: это НЕ пикселизация 3D.** На референсах сцена рендерится чисто, края
гладкие, лестницы пикселей на силуэтах нет. Ретро-ощущение дают четыре другие вещи:

1. **Низкополигональная геометрия.** Персонажи и предметы собраны из крупных плоскостей,
   фаски видны. Экономия идёт на моделях, а не на разрешении экрана.
2. **Плоские материалы без PBR-блеска.** Почти нет бликов, нет влажных отражений,
   нормали если и есть, то еле заметны. Цвет делает всю работу.
3. **Жёсткий точечный свет в темноте.** Ночной кадр собран из нескольких тёплых пятен
   и неоновой полосы на чёрном фоне. Пустота между источниками — часть стиля.
4. **Весь пиксель — в интерфейсе.** Крупный моноширинный шрифт, жёсткие рамки,
   пунктирные выделения, окна «под старую ОС», текст поверх кадра без полупрозрачности.
   Интерфейс рендерится в полном разрешении и читается идеально.

Отсюда поправка к пункту 1 выше: агрессивный масштаб рендера 0.5–0.6 — мимо референсов.
Держать 0.85 ради кадров и лёгкого замыливания, а стиль вытаскивать геометрией, плоскими
материалами, светом и интерфейсом.

Порядок работ после уточнения:

- **Материалы.** В `MaterialLib.get_material` сейчас включены `normal_enabled`, `roughness_texture`,
  `ao_enabled` (`ao_light_affect 0.7`) и `metallic_texture` для 14 наборов. Под новый курс нужен
  переключатель «плоский режим»: только альбедо, ровная `roughness` около 0.9,
  `metallic` 0, нормали и металл-карты отключены. Это же сразу снимает десятки текстур
  из памяти и работает на блок 5.
- **Свет.** Меньше ровного амбиента, больше отдельных ламп с чёткими пятнами;
  неон как единственный цветной акцент в кадре.
- **Интерфейс.** `UITheme` уже сидит на моноширинном JetBrainsMono — это попадание
  в референс. Что менять: `RADIUS_SM/MD/LG` (6/10/14) на ноль — углы должны быть прямыми,
  `BORDER_WIDTH` с 1 до 2–3, фон панелей сделать непрозрачным, добавить пунктирную рамку
  выбранного пункта. Файл: `game/UITheme.gd`, тема `ui/museum_theme.tres`,
  `theme/default_font_size=17` в `project.godot`.
- **Персонажи.** На референсах в кадре есть люди с простыми лицами-текстурами.
  У нас живого Куратора в кадре пока нет — этот подход делает его реализуемым силами
  одного человека.

#### Канон визуального стиля (задан пользователем 2026-08-02, считать обязательным)

Это описание пользователь дал дословно. Любые визуальные решения сверяются с ним;
если правка ему противоречит — правка неверная, а не канон.

Общее: ретро-хоррор с трёхмерной графикой в эстетике конца 1990-х — начала 2000-х,
уровень PlayStation 1 и ранних компьютерных 3D-игр. Картинка намеренно низкого
разрешения, с лёгкой пикселизацией, зернистостью и слегка размытыми текстурами.

Персонажи: простые low-poly модели, угловатые лица, почти без эмоций. Простота
должна давать эффект зловещей долины — обычный человек выглядит подозрительно
ещё до того, как что-то произойдёт. Анимации слегка скованные, нарочито
несовершенные. Гладких, «дорогих» анимаций не делать.

Свет: тусклые люминесцентные лампы с болезненным зеленовато-жёлтым оттенком в
помещениях; за окном почти абсолютная темнота, редкие фонари, густые чёрные тени.
Плотный ровный ambient — прямое нарушение канона.

Палитра: приглушённая и грязная — болотный зелёный, выцветший бежевый, серый,
тёмно-синий, тусклый жёлтый. Яркий цвет допускается точечно: вывеска, упаковка,
кровь, тревожный красный свет. Больше двух ярких пятен в кадре — перебор.

Интерфейс: крупный белый пиксельный шрифт, чёрные прямоугольные окна диалогов,
минималистичные значки взаимодействия, простые показатели. Скруглений нет.

Эффекты: никакой современности и фотореализма. Нужны намеренная техническая
грубость, аналоговый шум (VHS-зерно), слабое освещение, ощущение дешёвой ночной
видеозаписи. Экранные отражения, мягкое глобальное освещение, сильный bloom и
физически корректные материалы противоречат канону.

Ключевые слова для быстрой сверки: first-person retro horror, PS1-style 3D,
low-poly characters, pixelated low-resolution textures, fluorescent lighting,
muted green-yellow palette, deep nighttime shadows, VHS-like grain, uncanny human
faces, minimal pixel-font interface.

Важная оговорка по масштабу рендера: канон требует лёгкой пикселизации, но не
каши. Проверенные значения `scaling_3d/scale`: 0.55 — слишком грубо, читаемость
теряется; 0.85 — пикселя почти не видно; 0.75 — текущее рабочее значение.
Менять только вместе с осмотром кадров, не на глаз.

#### Что сделано по свету (первый шаг блока 9)

Всё в `_add_world_env()` в `game/FirstMuseumMap.gd`:

- `ambient_light_energy` 1.0 → 0.42, добавлены `ambient_light_sky_contribution = 0.55`
  и грязно-зелёный `ambient_light_color`. Раньше ambient вытягивал каждую тень в
  читаемый серый — ровно то, чего канон не допускает.
- `tonemap_white` 1.3 → 1.15.
- Glow: интенсивность 0.28 → 0.16, сила 0.62 → 0.50, bloom 0.05 → 0.0, порог
  0.9 → 1.15. Теперь светятся только настоящие источники света, а не яркие стены.
- Грейд: насыщенность 0.94 → 0.76, контраст 1.08 → 1.20, яркость 1.0 → 0.96.
- SSAO усилен (радиус 0.8, интенсивность 2.0, power 1.5) — единственный современный
  эффект, который стилю помогает: он сажает low-poly геометрию на пол.
- `ssr_enabled` и `ssil_enabled` выключены. Отражения мазали глянец по матовым
  полам, SSIL подсвечивал тени, которые должны быть чёрными. Заодно это выигрыш
  по производительности — засчитывается в блок оптимизации.
- Солнце: энергия 1.4 → 1.15, цвет чуть обесцвечен.
- Офисная лампа: цвет с бело-голубого на болезненный зеленовато-жёлтый
  (0.88, 0.95, 0.72), энергия 1.7 → 1.9, радиус 12 → 9 — светит своё пятно, а не
  всю комнату. Эмиссия трубки перекрашена в тот же оттенок.

Что по свету осталось:

- Уличные фонари: сделать редкими и жёсткими, между ними — настоящая темнота.
- Зернистость (VHS-шум) отдельным экранным шейдером — в проекте её ещё нет.
- Перебрать освещение вестибюля и атриума (`LobbyProps.build_lobby_lighting :466`).
- Аварийный красный — оставить как единственное яркое пятно на затемнении.

### Блок 10 — UI-система «МНЕМОС» (концепция задана пользователем 2026-08-02, правки согласованы)

Пользователь предложил развёрнутую концепцию интерфейса и согласился со всеми
поправками агента. Ниже — итоговый, уже отревизованный вариант. Читать его как
техническое задание, а не как черновик.

#### Главная установка

Интерфейс Museum Liminal НЕ копирует Shift At Midnight, Windows 95 и обычный
зелёный CRT. Они — только общее направление ретро-хоррора.

Внутримировая легенда стиля: **советский научный музей будущего, основанный в
конце XX века и реконструированный в 2003 году**. Поэтому в одном экране
сосуществуют пять слоёв:

1. старая монохромная терминальная основа;
2. серые промышленные окна 1990-х;
3. бледно-синие музейные панели начала 2000-х;
4. современные элементы, добавленные поверх старой системы;
5. аномальные символы, которые терминал физически не должен уметь рисовать.

Пятый слой — не украшение. Он объясняет искажения: железо 1994 года не умеет
этот глиф, а глиф есть.

#### Разрешённый конфликт с каноном визуального стиля

Канон (см. раздел выше) требует «крупный белый пиксельный шрифт, чёрные
прямоугольные окна». Концепция UI требует собственного стиля. Решение принято и
менять его без нового указания пользователя нельзя:

- **пиксельный шрифт остаётся** — он даёт эпоху и он в каноне;
- **своими делаем палитру, компоновку и логику панелей**.

В проекте сейчас `res://fonts/JetBrainsMono.ttf` и `res://fonts/Oswald.ttf`, оба
современные. Пиксельного шрифта в проекте НЕТ. Его добавление блокирует весь
остальной блок 10 — делать первым.

#### Палитра

| Роль | HEX |
| --- | --- |
| Графитовый фон | `#111518` |
| Тусклый сине-серый | `#435865` |
| Музейный бежевый | `#C6B994` |
| Холодный зелёный терминала | `#81A88A` |
| Аварийный янтарный | `#D59A42` |
| Опасный красный | `#B54A42` |
| Цвет разрыва (бледный фиолетово-белый) | `#C7B7D9` |

Правила использования (согласованы, не нарушать):

- Базу несут только первые четыре цвета.
- Янтарный, красный и фиолетово-белый — редкие. **Не более одного из этих трёх
  на экране одновременно.** Иначе через месяц интерфейс снова станет цветным
  салатом.
- **Зелёный означает только «система работает нормально».** Сейчас в
  `UITheme.gd` `ACCENT = 33c76b` стоит на всём подряд — кнопки, рамки, фокус,
  прогресс, — и потому ничего не значит. После правки исчезновение зелёного
  должно читаться как событие без единой строки текста.
- Тревога никогда не передаётся только цветом. Всегда плюс форма, рамка,
  подпись или ритм.

Аномалии временно ломают палитру, каждая по-своему:

- временной сдвиг — оставляет предыдущие состояния текста;
- фазовый провал — смещает отдельные слои интерфейса;
- зеркальный разлом — отражает только служебные подписи;
- жёлтый предел — постепенно заменяет цвета жёлтым и чёрным;
- радиационное цветение — выжигает отдельные пиксели экрана.

`Reduced flashes` не отключает атмосферу целиком: быстрые вспышки заменяются
медленным изменением яркости и статичными предупреждающими рамками.

#### Четыре уровня интерфейса

**1. Минимальный HUD наблюдателя.** Постоянно показывать только: время до
расширения разрыва, текущую задачу, название прибора в руках, маленький
индикатор воздействия аномалии, контекстное действие.

НЕ показывать постоянно: карту, здоровье, список камер, тип аномалии. Подробности
игрок получает только через физические устройства в мире.

```
СМЕНА 02  /  01:43:18

НАРУШЕНИЕ СТАБИЛЬНОСТИ
Осталось: 02:31

[E] ОСМОТРЕТЬ ЭКСПОНАТ
```

При приближении Куратора НЕ выводить «Враг рядом». Вместо этого: задержка
курсора, пропуски цифр таймера, строка `НАБЛЮДАТЕЛЬ: 2` при одном игроке,
второй маркер оператора в углу.

**2. Терминал диагностики МНЕМОС.**
`MNEMOS — Museum Nonlinear Event Monitoring and Observation System`,
ру-локализация `МНЕМОС — музейная система наблюдения нелинейных событий`.

```
МНЕМОС / ПОСТ НАБЛЮДАТЕЛЯ 01
────────────────────────────────────────

СОБЫТИЕ:     НЕ КЛАССИФИЦИРОВАНО
КРЫЛО:       SPACE / C
НОСИТЕЛЬ:    ТРЕБУЕТСЯ ПОДТВЕРЖДЕНИЕ
СТАБИЛЬНОСТЬ ██████░░░░ 61 %

ПРИЗНАКИ:
[✓] локальное нарушение геометрии
[✓] смещение отражённого света
[?] изменение массы
[ ] временная рекурсия

РЕКОМЕНДУЕМЫЙ ПРОТОКОЛ:
ПОДТВЕРДИТЬ НОСИТЕЛЬ ЧЕРЕЗ CCTV
```

Ключевое: терминал НЕ выдаёт готовый ответ обычным текстом. Он даёт диагноз
через набор признаков. Ночь 1 — система решает сама. Ночь 3 — часть данных
повреждена, игрок сопоставляет признаки руками. Это чинит реальную дыру: сейчас
терминал работает как кнопка «узнать правильный ответ».

**3. Планшет CCTV** — музейный инвентаризационный прибор, а не сетка камер.
Слева: схема крыльев, номера камер, состояние соединения, маркеры уже
проверенных экспонатов. Справа: большая камера, название экспоната, инвентарный
номер, спектр повреждения, кнопка удержания для подтверждения.

Надпись `СКАНИРОВАНИЕ 2,5 С` заменить последовательностью стадий:
`СОВМЕЩЕНИЕ КАДРОВ` → `СРАВНЕНИЕ С АРХИВОМ` → `ПОИСК НЕСООТВЕТСТВИЙ` →
`ПОДТВЕРЖДЕНИЕ НОСИТЕЛЯ`. Копеечная правка, а 2.5 секунды перестают быть налогом.
**СДЕЛАНО 2026-08-02**, см. шаг 6 блока 10.

Обязательная деталь: **архивная фотография экспоната рядом с текущим кадром.**
Иногда архивное фото само начинает меняться — аномалия переписывает прошлое.

**4. Интерфейс приборов.** ВАЖНО, поправка агента: двенадцать уникальных экранов
делать НЕ нужно, это недели работы на двух языках с масштабированием и высоким
контрастом. Схема: **одна общая рамка прибора + один сменный датчик внутри.**
Шесть типов датчиков на двенадцать приборов:

- вертикальный уровень с отклонением в градусах (гравитационный якорь);
- три наложенные временные шкалы (хроностабилизатор);
- расстояние и контур разрыва (спектральная линза);
- температура поверхности и след (термомел);
- шкала подавления наблюдаемого объекта (нуль-фонарь);
- механический счётчик заряда (катушка памяти).

#### Механика «Аномалия наблюдает за наблюдателем»

Чем больше игрок смотрит на аномалию через разные системы, тем точнее диагноз —
и тем больше аномалия узнаёт об игроке.

Параметр «обратное наблюдение» растёт, когда игрок: долго рассматривает разрыв;
повторно открывает одну камеру; использует спектральную линзу; долго держит
Куратора в поле зрения; читает повреждённые записи в терминале.

**Поправка агента, обязательна к исполнению:** параметр не должен быть полностью
скрытым. Скрытые счётчики работают только тогда, когда игрок задним числом может
сказать «а, так вот за что». Роль индикатора играет «маленький индикатор
воздействия аномалии» из HUD — без подписи и без цифр.

**Вторая поправка:** семь проявлений сразу — перебор. В первую итерацию идут
только три, они несут всю идею:

1. камера показывает игрока в помещении, где его сейчас нет;
2. в терминале появляется история действий, которых игрок ещё не совершал;
3. на схеме музея возникает дополнительная комната.

Отложены до того, как сыграют первые три: архивные карточки с лицом наблюдателя;
Куратор смотрит прямо в объектив; копия офиса охраны в карманном измерении;
вопрос системы `ПОДТВЕРДИТЕ НОСИТЕЛЬ АНОМАЛИИ: [ЭКСПОНАТ] [КУРАТОР]
[НАБЛЮДАТЕЛЬ]` на третьей ночи.

#### Финал

После закрытия последнего разрыва терминал сообщает о расхождении: устранённых
событий на одно меньше, чем зарегистрированных закрытий. На CCTV игрок видит
открытую дверь музея и собственную фигуру, уходящую на улицу, — но сам он ещё в
офисе. Чтобы завершить смену, нужно определить настоящего наблюдателя.

**Поправка агента №1: пересчитать числа.** В исходной идее было «6 устранено /
7 закрытий», но в проекте три ночи и десять аномалий. С чужой цифрой ошибка
«+1» не прочитается как ошибка. Числа брать из реального цикла.

**Поправка агента №2:** вариант ответа «тот, кто смотрит через экран игрока»
ломает четвёртую стену, и это единственное такое место в игре. Либо готовим
почву заранее (терминал раз-два обращается к игроку, а не к персонажу), либо
этот вариант убираем. Иначе читается как шутка.

#### Разделение работ — не смешивать

Концепция распадается на две разные вещи, и мешать их нельзя, иначе получится
наполовину переписанный UI поверх наполовину переписанной механики, и ни то ни
другое нельзя будет протестировать.

**Слой оформления → блок 10 (этот раздел).** Пиксельный шрифт, палитра,
`UITheme.gd`, `ui/museum_theme.tres`, прямоугольные рамки, стадии сканирования,
признаки в терминале, HUD, планшет CCTV, рамка прибора с шестью датчиками.
Правятся файлы тем и текстов.

**Слой механики → блок 8 (ревизия измерений).** Обратное наблюдение, новый
финал, диагноз по признакам как игровая система. По одной подсистеме за заход.

#### Порядок работ блока 10

1. Найти и добавить пиксельный шрифт с полной кириллицей (блокирует всё
   остальное). Кандидаты искать среди шрифтов со свободной лицензией и
   поддержкой ru/en.
2. `UITheme.gd`: `RADIUS_SM/MD/LG` `6/10/14` → `0`; `BORDER_WIDTH` `1` → `2–3`;
   непрозрачный фон панелей; пунктирная рамка выделения.
3. Переложить палитру на семь цветов выше, развести смыслы зелёного.
4. Собрать `MuseumTheme.tres` как общий + вариации `TerminalPanel`,
   `CCTVPanel`, `WarningPanel`, `InstrumentPanel`.
5. Аномальные эффекты накладывать ПОВЕРХ интерфейса, не трогая базовую
   разметку — иначе поедут RU/EN и `large_text`.
6. Стадии сканирования и признаки в терминале (тексты + логика показа).
7. Проверить читаемость на CCTV и мелком шрифте, прогнать регрессы.

Требования, которые нельзя потерять при любой правке: полноценные RU и EN;
текстовые области расширяются под длинные русские строки; общий коэффициент
масштабирования интерфейса; режим высокого контраста; уменьшение вспышек;
мышь + клавиатура + геймпад; анимации не ломают расположение Container-узлов
Godot; всё реализуемо через Control, Container и Theme; искажения всегда
сохраняют читаемость критической информации.

#### Название

«МНЕМОС / MNEMOS» закрывает половину блока 6: музей, система и игра могут
называться одинаково — как *Talos*, как *Alan Wake*. Коллизии в Steam на
2026-08-02 не проверялись, проверить перед фиксацией. Пользователь пока не
выбрал название окончательно.

#### Что сделано по блоку 10 — шаги 1–4 закрыты (2026-08-02)

**Шаг 1 — шрифт. Выбран Departure Mono.**

- Файлы: `fonts/DepartureMono.otf` + `fonts/DepartureMono-OFL.txt`.
- Автор Helena Zhang, лицензия **SIL OFL** (коммерческое использование разрешено), версия 1.500,
  источник `https://departuremono.com/assets/DepartureMono-1.500.zip`.
- Моноширинный пиксельный, Godot при импорте сам пишет `Pixel font detected,
  disabling subpixel positioning / hinting` — это признак, что шрифт взят верно.

**Как проверяли кириллицу и почему это обязательно.** Отсутствие глифа нигде не падает с
ошибкой: и Godot, и Pillow молча рисуют `.notdef` (пустота или квадратик). Поэтому есть
`tools/tmp_font_check.py`: он рендерит каждый нужный символ и сравнивает его битмап с
битмапом заведомо отсутствующего `U+FFFF`. Совпало — глифа нет.

    python tools\tmp_font_check.py            # проверить fonts/
    python tools\tmp_font_check.py <папка>   # проверить кандидатов

**Отбракованные кандидаты — повторно не проверять:**

- **Pixel Operator** (CC0) — кириллицы нет вообще, ни одной буквы, хотя в описании
  шрифта заявлена поддержка 44 языков. Главная ловушка этого шага.
- **Pixelify Sans** (OFL) — дыры в заглавных.
- **Pixellari** (OFL) — кириллица есть, но пропорциональный и без тире.
- **Handjet** (OFL) — кириллица полная, но декоративный сегментный, не «терминал».

**⚠ В Departure Mono НЕТ символов `✓` (U+2713) и `✗` (U+2717).** Это прямо бьёт по
исходной задумке терминала с признаками `[✓][✓][?][ ]`. Строки признаков строить
либо на ASCII — `[+] [x] [?] [ ]`, либо рисовать галку вручную. Стрелки (`→` в
целях испытаний) в шрифте есть, их менять не надо.

**Шаг 2 — базовая геометрия темы.** Правки в `game/UITheme.gd`:

- `MONO_FONT_PATH` и `DISPLAY_FONT_PATH` → оба на `res://fonts/DepartureMono.otf`.
  Старая пара JetBrains Mono + Oswald — два современных шрифта, именно они давали
  ощущение стокового Godot. Теперь один голос, иерархия через размер и регистр.
- `RADIUS_SM/MD/LG`: `6/10/14` → **`0/0/0`**. Имена оставлены, чтобы не править вызовы.
- `BORDER_WIDTH`: `1` → **`2`**, `FOCUS_WIDTH`: `2` → **`3`** (при `scaling_3d 0.75` пиксельная
  рамка исчезает на экранах CCTV).
- Старые `fonts/JetBrainsMono.ttf` и `fonts/Oswald.ttf` ПОКА НЕ УДАЛЯТЬ: на них ссылаются
  длинные док-комментарии с расчётами метрик в `TerminalType.gd`, `Cutscene.gd`,
  `SettingsPanel.gd`, `TerminalFrame.gd`. Их надо пересчитать под новый шрифт и только
  потом выкидывать файлы.

**⚠ Тема — это СОХРАНЁННЫЙ ФАЙЛ, правки `UITheme.gd` сами по себе ни на что не влияют.**
`project.godot` через `gui/theme/custom` грузит `ui/museum_theme.tres`. После любой
правки токенов обязательно:

    "<godot>" --headless --path . --script res://tools/tmp_save_theme.gd

Скрипт `tools/tmp_save_theme.gd` лежит в репозитории под `.gitignore` (`tools/tmp_*`) —
если его нет, пересоздать: `preload("res://game/UITheme.gd")` и `save_theme()`, именно
`preload`, т.к. `class_name` в голом `--script` не регистрируется.

Проверено: в `ui/museum_theme.tres` один `ext_resource` на `DepartureMono.otf`,
`border_width` везде 2 или 3, `corner_radius` не сериализуется вообще (все нули).
Регрессы после правок зелёные: `[OK] All tests passed.`

**Не скачивать шрифты в `tools/tmp_font/` внутри проекта.** Godot при `--import`
втягивает всё, что лежит под корнем, и создаёт `.import` на каждый мусорный ttf
(было 15 штук). Папка удалена.

**Шаг 3 — палитра. ЗАКРЫТ.** `UITheme.gd` переложен на семь канонных цветов блока 10:
`SURFACE #111518`, `PANEL_EDGE #435865`, `BEIGE #C6B994`, `ACCENT/SUCCESS #81A88A`,
`WARNING #D59A42`, `DANGER_FILL #B54A42`, `RIFT #C7B7D9`. Старый `ACCENT 33c76b` больше
не стоит на всём подряд — зелёный оставлен за смыслом «система в норме».

Два цвета канона не проходят по контрасту как текст, поэтому разведены на пару
токенов каждый — это сознательное решение, не отступление от канона:

- `DANGER_FILL #B54A42` — только заливка (жёсткая рамка и штриховка тревоги),
  слова несёт осветлённый `DANGER #E38175`;
- `PANEL_EDGE #435865` — только декоративная обвязка (разделители, неактивные рамки,
  сетка камер), всё, что игрок должен видеть и нажимать, идёт на `BORDER #6B8896`.

Контраст считался по WCAG 2.1, а не на глаз; таблица замеров лежит в шапке `UITheme.gd`.
Закрытые ворота: `SURFACE_RAISED : SURFACE` 1.42 : 1 (было 1.08), `BORDER : SURFACE`
4.87 : 1 (было 1.61). Все текстовые токены проходят 4.5 : 1 на обоих фонах.

**Шаг 4 — вариации темы. ЗАКРЫТ.** В `build_theme()` заведены четыре вариации поверх
общей темы: `TerminalPanel`, `CCTVPanel`, `WarningPanel`, `InstrumentPanel`
(`UITheme.gd :536–542`). Каждой явно задан `base_type = "Panel"` — без него незаданный
элемент проваливается в `Control`, а не наследует контракт `Panel`. Все четыре
сериализованы в `ui/museum_theme.tres` (`*/base_type` + `*/styles/panel`).

Кто уже сидит на вариациях: `DesktopWindow :109` и обои рабочего стола
`DesktopShell :92` — `TerminalPanel`; панели задач `DesktopShell :136/:168` и приборный
HUD `Compass :794` — `InstrumentPanel`; баннер наблюдения `GameplayEnhancements :824` —
`WarningPanel`; рабочее место CCTV `SecurityCameraTablet :721` — `CCTVPanel`;
рамка испытания `RiftTrialManager :1212` — `InstrumentPanel`. `TerminalFrame :898`
принимает вариацию параметром и откатывается на `TerminalPanel`, если пришло чужое имя.
Покрыто регрессом: `test_project_integration :41–54` проверяет наличие всех четырёх
в теме и то, что CCTV выбирает именно `CCTVPanel`.

**Миграция `add_theme_*` — закрыта, цифра 151 устарела.** Пересчитано по коду:
в `game/*.gd` осталось 115 вызовов — 55 `constant_override` (отступы и промежутки,
токена для них нет), 41 `stylebox_override` (стили, собранные из тех же токенов и
подменяемые в рантайме), 13 `color_override`, 2 `font_override` в `TerminalType`
(институциональное начертание) и 4 упоминания в комментариях самого `UITheme.gd`.
Ни одного `add_theme_font_size_override` вне `UITheme.gd` не осталось. То есть
разрозненных стилей больше нет: остаток — это геометрия и рантайм, а не палитра.

**Сверка с референсом 01 — СДЕЛАНА 2026-08-02.**

Первое: **пути референсов в этом документе были неверные.** Файла
`references/01_terminal.jpg` не существует. Всё лежит в `references/mnemos_ui/`:
оригиналы `01_terminal.jpeg` … `13.png` (600 КБ – 2 МБ, агенту не по зубам) и
уменьшенные копии `references/mnemos_ui/small/*.jpg` по 25–29 КБ — читать через
`file_read_media` можно только их. Критерии приёмки — в `references/mnemos_ui/README.md`,
промпты на недостающие `10_end_shift_report` и `12_anomaly_distortion` — в
`PROMPTS_PENDING.md`.

Итог сверки. Из шести блоков референса в коде есть пять: сводка события
(`TERM_FIELD_EVENT`, `GameManager :2636`), стабильность (`add_stability_bar`, :2639),
признаки (`TERM_SECTION_SYMPTOMS`, :2641), рекомендуемый протокол
(`TERM_SECTION_PROTOCOL`, :2650) и журнал событий — правда, отдельным окном
(`DESK_TITLE_LOG`, :3011), а не блоком внутри терминала.

**Шестой блок — план музея — ДОБАВЛЕН 2026-08-02.** Раньше план этажа жил только
в компасе HUD (`Compass.gd`, `COMPASS_MAP_HINT` — «M — удерживать») и в планшете
CCTV. Теперь он есть и в отчёте: `TerminalFrame.add_floor_plan()` /
`set_floor_plan()` рисуют блок, `GameManager._proto_plan` его заводит
(`add_floor_plan(column, "TERM_SECTION_PLAN")`) и наполняет из
`_refresh_protocol_plan()`. Новый ключ каталога один: `TERM_SECTION_PLAN`
(«ПЛАН МУЗЕЯ» / «MUSEUM PLAN»), каталог 505 → 506.

Решения, которые тут приняты:

- **Таблица комнат не копируется.** `SecurityCameraTablet.ROOMS` остаётся
  единственным владельцем одиннадцати прямоугольников; `GameManager._floor_plan_rooms()`
  читает её в рантайме через `get_script_constant_map()` — ровно тем же способом
  и по той же причине, что `Compass._load_rooms()`. Разошедшийся с геометрией план
  хуже отсутствующего: игрок ему верит.
- **`TerminalFrame` ничего не знает о музее** — он рисует те прямоугольники,
  которые ему дали. Одна таблица, три рисовальщика.
- **Метка — аномалия, а не игрок.** Это отчёт о событии, читаемый за столом;
  «где стою я» — вопрос компаса, и у компаса на него свой живой план. Так
  снимается и запрет `Compass._should_show()` на два плана разом.
- **Акцент не тратится.** Комната происшествия выделена скобками вокруг названия,
  подложкой `SURFACE_RAISED` и крестом на точке; запертые крылья перечёркнуты.
  Ни цвет в одиночку, ни второй акцент на странице не нужны.
- Блок целиком (заголовок + линейка + план) прячется сам, если таблица не
  загрузилась, — заголовок над пустотой читался бы как поломка.

Композиция расходится сознательно: в референсе — единый полноэкранный пост,
у нас — оконный десктоп. README сам говорит, что референсы — материал для
оценки композиции, плотности и ритма, а не макеты, поэтому окна остаются.
А вот плотность была ниже референсной — исправлено, см. ниже.

**Плотность рабочего стола — СДЕЛАНО 2026-08-02.** `DesktopShell._place()` больше не
валит окна лесенкой друг на друга. Новая `_free_spot()` обходит стол по сетке
обоев (32 px) слева направо, сверху вниз и кладёт окно в первую клетку, где
оно влезает целиком и никого не задевает (`_occupied_rects()`; свёрнутые окна
места не держат). Лесенка `_next_cascade()` осталась запасным вариантом на
случай, когда свободного места нет. Зазора между окнами нет нарочно:
`Rect2.intersects()` без `include_borders` не считает касание пересечением, и рамки
соседей складываются в одну линию, как на референсе.

Размеры окон подогнаны под экран 1600×900 (слой окон — 1600×844 без строки
состояния 26 и панели задач 30), всё кратно 32: протокол 768×512 → 1088×576,
питание 448×288 → 512×288, журнал 384×256 → 512×352. Кладётся точно: протокол
слева, справа колонка 512 (питание над журналом), 1088 + 512 = 1600.
Заполнение стола — ~71 % против ~53 % с перекрытиями до правки. Свободная
полоса 1088×268 внизу слева — готовое место под окно CCTV.

**Утечка янтаря в `TerminalFrame` — ЗАКРЫТА 2026-08-02.** `accent_color()` лерпил
`ACCENT → WARNING` по уровню повреждения, и янтарь утекал туда, где он ничего не
значит: горячая кромка полос выпадения, спеклы, сегменты шкалы целостности
и чип штатного ядра. Это ломало два правила сразу: README референсов
(«янтарный — только для стабильности», «зелёный — только штатная работа») и
«RULE OF ONE ACCENT» в шапке `UITheme.gd`. Теперь повреждение ГАСИТ акцент
(`ACCENT → ACCENT_DIM`), а не перекрашивает его в предупреждение. Где тревога
действительно есть, её называют явно: `_draw_meter()` сам ступает в `WARNING` на
0.4 и `DANGER` на 0.75, `_stability_color()` — на 0.6/0.3. Важно: `_core_color()` для
штатного ядра теперь возвращает ровный `ACCENT`, а не точку на рампе — это цвет
текста, а `ACCENT_DIM` даёт 3.15 : 1 на `SURFACE` и как надпись не проходит.

Все три файла (`DesktopShell.gd`, `GameManager.gd`, `TerminalFrame.gd`) проверены
`--check-only` — чисто. Глазами новая раскладка ЕЩЁ НЕ СНЯТА: скрипта, который
делает именно `shots/view/ui_desktop.jpg`, в проекте нет — прошлый кадр снят вручную,
а capture нельзя гонять с `--headless`.

**Шаг 5 — аномальные эффекты. ЗАКРЫТ 2026-08-02, но не так, как был записан.**
Формулировка «наложить эффекты» устарела: весь слой был написан в `TerminalFrame.gd`
ещё на этапе 9.1 — MURK, палитра, ломающиеся линейки, падающая целостность,
DROPPED LINES (`BAND_MAX 7`), SPECKLE (`SPECK_MAX 220`), CHARACTER ROT
(`GLITCH_MAX_RATIO 0.25`, `GLITCH_ALPHABET`), панель потери сигнала, всё на
`GLITCH_HZ 12.0` и с честным поведением при `reduced_flashes`. Проводка тоже
оказалась на месте: `GameManager._night_corruption()` даёт устойчивый пол
(затемнение + `CORRUPT_PER_NIGHT` за ночь + `CORRUPT_TIMER_SPAN` по мере
истечения окна сдерживания; провал 1.0, победа 0.0), а `_sync_terminals()`
бьёт `pulse_corruption()` от близости Куратора внутри `CURATOR_PULSE_RANGE`.
Лгала только шапка `TerminalFrame.gd`: там до сих пор стояло «WIRING IS THE NEXT
PHASE'S JOB. Nothing in the game calls set_corruption()». Шапка переписана по факту.

Что действительно осталось неподключённым — `set_signal_lost()`, и это
сознательно: он заменяет тело страницы рваной картинкой с одним сообщением,
а значит на экране провала спрячет `FAIL_RETRY` (единственную строку о том, как
уйти), а на протоколе — имя прибора, ради которого страница и существует. Ему
нужна своя поверхность, которой пока нет. Причина записана над `_night_corruption()`.

Новый блок плана намеренно встроен в этот же слой: он лежит под `_noise`, а его
собственные чернила идут по тому же ramp'у (`chrome_color` — стены,
`secondary_color` — перечёркнутые крылья, `primary_color` — названия), и
`_render()` перерисовывает планы вместе с остальной страницей.

**Шаг 6 — стадии сканирования и признаки. ЗАКРЫТ 2026-08-02.**

Половина шага уже стояла: признаки в терминале давно рисуются ASCII-метками
`MARK_PRESENT [+]`, `MARK_ABSENT [x]`, `MARK_UNKNOWN [?]`, `MARK_PENDING [ ]`
(`TerminalFrame.gd :696–699`) — как и требует Departure Mono, в котором нет
`✓` и `✗`. Трогать не пришлось.

Вторая половина сделана: `HUD_SCAN_PROGRESS` («СКАНИРОВАНИЕ: %.0f%%») удалён из
каталога и из кода. Вместо процента `GameplayEnhancements` показывает стадию —
`SCAN_STAGE_KEYS` (`HUD_SCAN_ALIGN` → `HUD_SCAN_ARCHIVE` → `HUD_SCAN_MISMATCH` →
`HUD_SCAN_CONFIRM`), стадия выводится из `_scan_progress` функцией
`_scan_stage_key()`. Каталог 506 → 510.

Решения:

- **`SCAN_TIME` не тронут.** 2.5 секунды остались 2.5 секундами: правка про то,
  чем эти секунды заняты на экране, а не про длину ожидания. Баланс ночи 2 и 3
  и текст `HUD_CONFIRM_SOURCE_FIRST` («ещё %.1f сек.») продолжают работать.
- **Стадия вычисляется, а не хранится.** `_update_scan()` откатывает прогресс на
  половинной скорости, когда оператор отвёл взгляд; запомненный индекс продолжал
  бы врать про «сравнение с архивом», пока шкала утекает к нулю.
- **Ключи без спецификаторов** — потому и разрешено брать их из массива:
  `tools/check_localization.py` видит арность только у литерала на месте вызова,
  а у стадий форматировать нечего.
- **Отдельный `HUD_SCAN_SEEK`** («ИСТОЧНИК НЕ ПОДТВЕРЖДЁН — СМОТРИТЕ КАМЕРУ %02d»)
  на случай, когда нужный пост не на экране. Без него стадии читались бы как
  работа машины сама по себе, пока игрок стоит в коридоре. Зеркалит уже бывший
  `HUD_EVIDENCE_SEEK` у девяти инцидентов с признаками, но своими словами: там
  «признаки», здесь «источник».
- Пример в шапке `TaskBlock.gd :359` переписан — он ссылался на удалённый ключ.

Осталось от блока 3 (планшет CCTV) и сюда не входит: архивная фотография
экспоната рядом с текущим кадром, которая иногда сама начинает меняться.

**Шаг 7 — читаемость и доступность. ЗАКРЫТ 2026-08-02.** Шаг был записан как
«проверить и прогнать регрессы», но проверка нашла настоящую поломку, и шаг
оказался работой, а не сверкой.

- **Найдено: `large_text` ломал раскладку стола.** `SettingsManager :203` ставит
  `content_scale_factor = 1.12`, и логический слой окон с 1600x844 ужимается до
  ~1428x748. Окна же открывались в АБСОЛЮТНЫХ пикселях (1088 + 512 = ровно
  1600 по ширине). На ужатом столе `_free_spot` не находил места справа,
  возвращал `null`, и `_next_cascade` клал питание с журналом лесенкой ПОВЕРХ
  протокола. То есть плотная раскладка, выверенная под референс 01,
  рассыпалась ровно у того, кто включил крупный текст, чтобы лучше видеть.
- **Решение: окна задаются долями стола, а не пикселями.** `DesktopWindow.desk_fraction`
  (`Vector2.ZERO` = старое абсолютное поведение), четвёртый необязательный
  аргумент `DesktopShell.open_window`, счёт размера в `_fit_size`. Доли:
  протокол `0.68 x 0.6825`, питание `0.32 x 0.3412`, журнал `0.32 x 0.4171`.
  На 1600x844 это байт в байт прежняя раскладка; на ужатом — 960x480,
  448x224, 448x288, и колонка снова встаёт рядом, а не поверх.
  Пиксельные константы остались запасным вариантом на первый кадр, пока слой
  ещё нулевой.
- **Округление только вниз** — отдельный `_snap_down` вместо `_snap` (тот
  `round()`). Две доли, округлённые вверх, вместе перелезают стол и отнимают
  место у соседней колонки. Эпсилон +0.5 спасает честные размеры
  (287.9999 -> 288), но не округляет 971.4 до 992.
- **Окно переезжает только если его место испортилось.** `_fit_window` ищет новое
  место только при `_rect_conflicts` (отрицательная позиция, выход за стол,
  пересечение с чужим несвёрнутым окном). Пока раскладка цела, окно стоит
  на месте: оператор должен знать, где журнал, не глядя.
- **`ignore` в `_free_spot`/`_occupied_rects`** — иначе при перепаковке окно
  находит занятым свой собственный прямоугольник и всегда сваливается в лесенку.
  В `_place` окна ещё нет в `_windows`, в `_fit_window` — уже есть.

Проверено и правки НЕ потребовало: `reduced_flashes` читают полтора десятка
честных потребителей (`TerminalFrame :1253/:1267`, `CRTOverlay :127`,
`SecurityCameraTablet :1764`, `TaskBlock :972`, `Compass :688`, `LightProps :621`,
`GameplayEnhancements :461/:619/:1153`, `FirstMuseumMap :3695`); бюджет контраста
расписан в шапке `TerminalFrame :195–216`; `high_contrast` работает через
`SettingsManager._apply_contrast`. Новый план музея рисуется под `_noise` и по тому
же ramp’у, отдельный гейт ему не нужен.

Проверки: `--check-only` по `DesktopShell.gd`, `DesktopWindow.gd`, `GameManager.gd` —
чисто; `510 keys in catalogue, 510 used in code / OK`; `Integration smoke test:
0 failure(s)`. **Глазами ПРОВЕРЕНО 2026-08-02** — см. ниже.

**Пересъёмка стола в двух режимах.** `tools/tmp_ui_shot.gd` теперь снимает два
кадра за один прогон: `ui_desktop` и `ui_desktop_large`. Крупный текст
включается через настоящую настройку (`settings_manager` из группы,
`set_large_text(true)`), а не правкой `content_scale_factor` руками — иначе
проверялся бы хвост цепочки, а не она сама. После съёмки настройка
возвращается: она пишется в `user://museum_settings.cfg` и иначе осталась бы
включённой для всех следующих кадров и запусков. Замеры с прогона:

- обычный стол `1600x900`: протокол `1088x576` @ `(0,0)`, журнал `512x352`
  @ `(1088,0)` — байт в байт прежняя раскладка, регресса нет;
- `large_text`, `content_scale_factor 1.12`, стол `1428.571x803.571`:
  протокол `960x480` @ `(0,0)`, журнал `448x288` @ `(960,0)`.
  960 + 448 = 1408 при 1428 доступных — колонка стоит РЯДОМ, а не поверх,
  что и было целью шага. Расчёт сошёлся с замером точно.

Окна питания на кадрах нет и быть не должно: `POWER_ENABLED := false`, его место
занимает журнал. Значит доля питания (`0.32 x 0.3412`) сама по себе ещё не
проверена в деле — проверится, когда питание включат.

Что видно глазами на `shots/view/ui_desktop_large.jpg`: два прямоугольных окна
в строку без нахлёста и без лесенки, рамки целые, подвал стола свободен.
Запас пустого места внизу при `large_text` больше, чем в обычном режиме —
это цена долей и округления вниз, и она правильная: лучше полоса обоев,
чем окно поверх отчёта. Мелкий текст внутри окон на уменьшенном JPEG
не читается — для сверки подписей плана музея и стадий сканирования нужны
плитки `shots/tiles/ui_desktop_large_r*c*.jpg`, это ещё не сделано.

**Окно наблюдения на столе — ДОБАВЛЕНО 2026-08-02.** Третья программа
рабочего стола, в ту самую свободную полосу под протоколом, которая была
под неё оставлена при уплотнении стола.

- Компоновка по референсу 04: слева перечень постов (`CCTV_LIST_WIDTH 232`),
  справа кадр. Метки те же, что у створок в окне питания: `[+]` — пост на
  экране, `[ ]` — пост есть, но не показан. Шум наводит сам фид; подписи
  и рамка остаются резкими, как требует README референсов.
- **Кадр берётся взаймы и честно возвращается.** Фид рисуется, только пока
  его кто-то держит (`SecurityCameraTablet.feed_texture` / `release_feed`), и не
  более `FEED_BUDGET_PER_FRAME` за кадр на всю сцену. `GameManager._sync_cctv_feed()`
  держит РОВНО ОДИН фид и отдаёт его, когда окно свёрнуто или оператор
  встал из-за стола. Сверка зовётся каждый кадр и выходит на первой строке,
  пока состояние не менялось: сигнала о сворачивании окна наружу нет.
- Перечень постов читается из `SecurityCameraTablet.CAMS` через
  `get_script_constant_map()` — тем же способом, что и план музея читает `ROOMS`.
  Одиннадцать постов, все ключи названий (`CAM_ENTRANCE` и далее) уже были
  в каталоге — новый ключ понадобился один: `DESK_TITLE_CCTV`.
- Доля стола задана сразу (`CCTV_WINDOW_FRACTION Vector2(0.68, 0.3175)`,
  `CCTV_WINDOW_SIZE Vector2(1088, 268)`) — без неё окно повторило бы поломку
  шага 7 при `large_text`.

Замеры пересъёмки с тремя окнами (`tools/tmp_ui_shot.gd`, оба режима):

- обычный стол `1600x900`: протокол `1088x576` @ `(0,0)`, CCTV `1088x256` @ `(0,576)`,
  журнал `512x352` @ `(1088,256)`;
- `large_text` (стол `1428.571x803.571`): протокол `960x480` @ `(0,0)`,
  CCTV `960x224` @ `(0,480)`, журнал `448x288` @ `(960,0)`.

Нахлёста нет ни в одном режиме, всё в границах стола. Высота окна села
на `256` вместо заказанных `268` — это `_snap_down` по сетке обоев (32),
так и задумано.

**Хвост, не закрытый в этом цикле:** с тремя окнами журнал в обычном
режиме встал не в верхний правый угол, а на `(1088,256)`, и над ним осталась
пустая клетчатая площадка `512x256`. При `large_text` того же эффекта нет.
Причина не разобрана: окна открываются в порядке журнал → CCTV → протокол,
первые два ставятся, пока слой ещё нулевого размера, а потом их
переставляет `_fit_all_windows` по `resized`. Лечить следующим циклом — смотреть
`DesktopShell._place` и порядок вызовов в `_enter_workstation` / `_show_protocol`.

Проверки цикла: `--check-only` по `GameManager.gd` — чисто;
`511 keys in catalogue, 511 used in code, locales: en, ru / OK`;
`Integration smoke test: 0 failure(s)`; оба кадра сняты заново.

**Семь шагов порядка работ блока 10 на этом закончились.** Шага 8 в списке
нет. Не закрытыми по блоку остаются не шаги, а хвосты: наполнение остальных
окон стола (датчики, настройки, тревога — окно CCTV ДОБАВЛЕНО 2026-08-02,
см. ниже), миграция `add_theme_*` на токены темы — ЗАКРЫТА 2026-08-02,
см. раздел ниже, — и список сверок глазами ниже.

## Шкала отступов в теме — ЗАКРЫТО 2026-08-02

Цифра «115 разрозненных вызовов `add_theme_*`» (как и «151» в шапке
`UITheme.gd`) оказалась устаревшей: собственная записка темы над
`build_theme()` уже говорила, что размеры и цвета мигрированы, а осталось
двое — стили, подменяемые на ходу и собранные ИЗ токенов, и отступы,
**для которых токена просто не было**. Поэтому работа была не в том, чтобы
переписать сто пятнадцать строк, а в том, чтобы завести недостающий токен.

Добавлено в `game/UITheme.gd`:

- шкала из четырёх шагов рядом с `PAD_X` / `PAD_Y`: `GAP_TIGHT 2` (строки
  одного показания), `GAP_ROW 6` (строки внутри блока), `GAP_BLOCK 10`
  (между блоками, значение по умолчанию), `GAP_SECTION 14` (между частями,
  которые должны читаться раздельно). Значения взяты те, что уже стояли на
  экране, — миграция строки была переименованием, а не переделкой;
- `apply_gap(container, amount, cross)` — прячет расхождение самого Godot:
  `BoxContainer` зовёт это `separation`, `GridContainer` и flow-контейнеры
  делят на `h_separation` / `v_separation`. Место вызова больше не обязано
  знать, что именно оно держит;
- `apply_inset(margin, x, y)` — четыре стороны одной строкой. Четыре
  рукописных вызова на один отступ — это ровно та форма, при которой три
  стороны верные, а четвёртая на 4 px мимо.

Отступы намеренно оставлены на `PAD_X` / `PAD_Y`, а не влиты в шкалу зазоров:
поле и зазор — разные величины, и общая шкала для них — это то, как поля
начинают уползать в зазоры.

Переведены на токены: `SettingsPanel` (карточка строки, врезка-выноска,
стык колонок), `Compass` (три панели прибора), `GameManager` (служебная
консоль, окна питания, журнала и CCTV), `DesktopWindow`, `InventoryBar`,
`TutorialPrologue`.

НАМЕРЕННО не тронуто, чтобы правка осталась переименованием:

- места, уже сидящие на своих именованных константах (`TASK_GAP`,
  `SECTION_GAP`, `ROW_GAP`, `CAP_GAP`, `FIELD_GAP`, `COLUMN_GAP`, `SLOT_GAP`,
  `OSD_GAP`, `CELL_GAP`, `BODY_PAD`, `BEZEL`, `STACK_SEPARATION`) — они
  описывают геометрию своего экрана, а не общий ритм;
- несимметричные врезки `SettingsPanel` (26/4/6/4 у колонки содержимого,
  24/4 у рейки) и значения 12 и 8, которых на шкале нет: подгонять их под
  токен значило бы двигать пиксели под предлогом уборки;
- стили, подменяемые на ходу (`_watch_style_near`, `_chip_active`, полосы
  прогресса и прочее) — они и должны собираться из токенов вызовом.

Проверки цикла: `--check-only` по `UITheme.gd`, `GameManager.gd`,
`SettingsPanel.gd` — чисто; `511 keys in catalogue, 511 used in code,
locales: en, ru / OK`; `Integration smoke test: 0 failure(s)`.

Главное доказательство того, что это была уборка, а не редизайн: стол
переснят в обоих режимах, и замеры совпали с прошлыми до пикселя — протокол
`1088x576` @ `(0,0)`, CCTV `1088x256` @ `(0,576)`, журнал `512x352` @
`(1088,256)`; при крупном тексте `960x480`, `960x224`, `448x288` @ `(960,0)`.

## Места окон на столе — ЗАКРЫТО 2026-08-02

Смещение журнала и пустой прямоугольник 512x256 в правом верхнем углу
— причина найдена и убрана. Это был не сбой раскладчика, а его честная
работа не по тому вопросу: `_free_spot` отвечает, куда окно ВЛЕЗЕТ, а не
куда оно ДОЛЖНО лечь. Отсюда две беды сразу: результат зависит от порядка
открытия окон, а самый первый проход идёт по столу нулевого размера (за
компьютер ещё не сели, слой не разложился), где свободного места нет
вовсе и все окна уходят в лесенку. Потом стол получает размер, окна
чинятся по одному в порядке открытия — и последнему достаётся остаток.

Показательно, что при крупном тексте журнал вставал ПРАВИЛЬНО: там
пропорции сложились иначе, и поиск случайно совпадал с замыслом. Раскладка,
верная по совпадению, — та же ошибка, просто пока не видная.

Сделано:

- `DesktopWindow.desk_anchor` — место окна в долях стола, рядом с уже
  существовавшим `desk_fraction` (размер в долях). Отрицательное значение —
  место не объявлено; ноль здесь не годится, левый верх — законное место;
- `DesktopShell.open_window(...)` принимает место шестым доводом
  (`desk_anchor`), `_anchor_spot()` считает его в пикселях, а `_place()` и
  `_fit_window()` предпочитают его поиску клетки. Поиск остаётся для окон
  без объявленного места — их будет ещё три (датчики, настройки, тревога);
- `GameManager`: `PROTOCOL_WINDOW_ANCHOR (0, 0)`, `CCTV_WINDOW_ANCHOR (0, 0.6825)`,
  `LOG_WINDOW_ANCHOR (0.68, 0)` — объявлены одной группой, потому что это одна
  раскладка, а не три независимых числа.

Вторая ошибка, всплывшая на первой же пересъёмке и тоже закрытая: место
округлялось `_snap` (к ближайшей клетке), а размер — `_snap_down` (вниз).
Два разных округления в одной колонке разошлись на клетку: при крупном тексте
протокол кончался на 480, а CCTV начинался с 512, и между ними светилась
полоса обоев. `_anchor_spot` теперь кладёт место на сетку тем же способом,
что и размер.

Замеры после правки (оба режима, щелей нет):

- обычный: протокол `1088x576` @ `(0,0)`, CCTV `1088x256` @ `(0,576)`,
  журнал `512x352` @ `(1088,0)` — было `(1088,256)`;
- крупный текст: протокол `960x480` @ `(0,0)`, CCTV `960x224` @ `(0,480)`,
  журнал `448x288` @ `(960,0)`.

Проверки: `--check-only` по `DesktopShell.gd`, `DesktopWindow.gd`,
`GameManager.gd` — чисто; `Integration smoke test: 0 failure(s)`; кадр
просмотрен глазами — три окна стоят плотно, без дыры в углу.

Также пересобран `ui/museum_theme.tres` (`tools/tmp_save_theme.gd`): без этого
шкала отступов из прошлого цикла жила бы только в коде, а тема на диске
оставалась старой.

**Проверки блока 10, ещё не закрытые глазами:**

- сверить нынешнее окно терминала с `references/mnemos_ui/small/01_terminal.jpg` — СВЕРЕНО 2026-08-02,
  итог ниже; осталось посмотреть новую раскладку глазами в игре;
- наполнить остальные окна десктопа по одному, через `reveal_window`, а не пачкой:
  CCTV (референс 04), датчики (07), настройки (11), тревога (09);
- разобрать снимок кнопки двери (`office_door_button.jpg`) — табло и укрупнённый
  пульт не оценены;
- пересъёмка макро-планов ламп после правки `cam.environment`;
- связь двери с Куратором: упирается ли он в закрытую створку и корректно ли уходит
  в обход (`game/OfficeDoor.gd`).

**Технический долг, накопленный к этому месту:**

- предупреждения движка на выходе: 8 `ObjectDB instances leaked at exit`,
  3 `resources still in use at exit`, ~170 × `SpatialMaterial remapped parameter
  not found: specular`;
- производительность после увеличения `FEED_SIZE` камер CCTV до 768×576 (9 фидов)
  не мерялась.

**Что ещё не занесено в этот документ из последних циклов** (файлы в репозитории есть,
разделы выше про них не знают): новый фиттинг окон и переписанный `game/DesktopWindow.gd`,
оболочка рабочего стола `game/DesktopShell.gd`, окно журнала событий, дверь в стиле FNaF
(`game/OfficeDoor.gd`), темп смены, `game/props/StatusLamp.gd`, каталог `references/`,
`game/TaskBlock.gd`. Локализация выросла до 505 ключей (`localization/game.csv`,
506 строк с заголовком) — цифры 398 и 474 в разделах 4 и 6 устарели, и раздел 4 ещё
считает `game/props/` за 17 файлов без `TreeLib.gd` и `StatusLamp.gd`.
Всё перечисленное на 2026-08-02 не закоммичено: `git status` держит 20 изменённых
файлов и полтора десятка untracked, включая `fonts/DepartureMono.otf`, `models/trees/`
и `tools/bake_trees.gd`.

### Разное, не привязанное к блокам

- Освещение вестибюля и атриума — `LobbyProps.build_lobby_lighting :466`.
- У `_cylinder` (`ExteriorProps :77`) нет параметра `pack` — при переводе экстерьера
  на текстуры его придётся добавить.
- Звук подтверждения шагов обучения (было: стингер закрытия разлома на первом же шаге)
  исправлен 2026-08-02 в `GameManager` (~:3114). Общее правило: `resolve` звучит только там,
  где игрок действительно что-то закрыл; мелкие подтверждения — `terminal_beep`.

---

## 15. Оценка масштаба и пересборка плана — 2026-08-03

Этот раздел ЗАМЕЩАЕТ порядок работ из раздела 7 и очерёдность блоков
раздела 13. Сами блоки не отменяются — меняется только порядок и границы.

### Честная оценка состояния

Сильное: мир и оболочка сделаны честно и подробно; инструментарий агента
(снимки, 13 тестов, проверка материалов и локализации) выше среднего по отрасли;
код дисциплинирован, решения записаны. Оболочка «МНЕМОС» доведена до
уровня, который встречаешь у выпущенных игр.

Главная беда одна, и она не техническая: **в эту игру никто никогда не играл
от начала до конца.** Сборки не существует ни разу, а автозагрузка
`_mcp_game_helper` смотрит на `addons/godot_ai`, вырезанный из экспорта, — то есть
первый же экспорт, скорее всего, упадёт на старте. Всё, что мы знаем об игре,
мы знаем из снимков и дымовых тестов. Они доказывают, что игра ЗАПУСКАЕТСЯ
и НЕ ПАДАЕТ. Они ничего не говорят о том, интересно ли в неё играть.

Остальные пробелы масштаба, по убыванию важности:

1. **Тишина.** Нет ни одного музыкального трека и нет субтитров. В хорроре
   звук даёт половину страха и почти всю подачу темпа. Это самый большой
   перекос вложенных сил: на картинку потрачены месяцы, на слух — ноль.
2. **У угрозы одна передача.** Куратор бинарен: замер или бежит по прямой.
   Страх живёт на разнице состояний — далёко/близко, ищет/нашёл, ушёл/вернётся.
   Одно состояние = один испуг за всю ночь, дальше привыкание.
3. **Ширина вместо глубины.** Десять измерений написаны до того, как хоть одно
   было признано хорошим. По словам самого пользователя — «у некоторых механики
   отвратные». Девять плохих измерений не складываются в одно хорошее.
4. **Два взаимоисключающих курса картинки идут одновременно.** Блок 4 требует
   2K PBR и уже набрал 1.67 ГБ в `models/`; блок 9 требует пиксельной картинки
   с текстурами 256–512 без сглаживания. Каждый час, потраченный на блок 4
   до решения по блоку 9, с большой вероятностью выброшен.
5. **Оболочка обогнала игру.** 511 ключей локализации на два языка для текстов,
   которые ещё могут поменяться целиком. Это не ошибка, но это проценты
   по долгу: каждая правка текста теперь стоит вдвое дороже.
6. **Нет определения «готово».** Нигде не записано, сколько ночей в игре, чем
   она кончается, сколько длится первое прохождение и где она выходит.
   Без конца любой список задач бесконечен, а любой блок кажется важным.
7. **Нет мид-сейва**, а ночь не встаёт на паузу внутри карманного измерения.
   Первое делает тестирование мучительным, второе ворует у игрока время ночи
   за то, что он честно делал задание.
8. **Работа не закоммичена.** 25+ изменённых файлов и ~15 неотслеживаемых
   висят в рабочей копии. Один неудачный день — и циклов работы как не бывало.

### Почему меняется порядок

Нынешний план отсортирован по видимости результата: двор, деревья, текстуры,
окна — то, что можно показать кадром. Но сортировать надо по РИСКУ: сначала
то, что может обесценить уже сделанное. Сейчас таких рисков три: сборка
может не запуститься вообще; цикл ночи может оказаться скучным; курс
картинки может отменить весь текстурный труд.

### Фазы (новый порядок)

**Фаза 0 — игра существует вне редактора.** Самая дешёвая и самая срочная.

- [СДЕЛАНО 2026-08-03] закоммитить накопленное в `wip/audit-2026-07-26`.
  Коммит `8314bee`, 44 файла (23 изменённых и остальные новые: шрифт
  DepartureMono, `DesktopShell`, `DesktopWindow`, `OfficeDoor`, `StatusLamp`, `TreeLib`,
  `models/trees/` 5.4 МБ, `references/` 12 МБ). Перед коммитом прогнаны:
  `check_materials.gd` (карт 59, наборов 14, ошибок 0), `test_project_integration`
  (0 failure(s)), `check_localization.py` (511/511, en+ru). Отладочный
  `theme_variants_step4.png` сознательно оставлен вне коммита;
- [СДЕЛАНО 2026-08-03] автозагрузка `_mcp_game_helper` больше не валит сборку.
  Было: прямая ссылка на `res://addons/godot_ai/runtime/game_helper.gd`, который
  вырезается строкой `exclude_filter="addons/godot_ai/*, notion_worker/*,
  game/test_*.gd"` в `export_presets.cfg` — в собранной игре автозагрузка
  указывала бы на несуществующий ресурс. Стало: новый `game/McpBridge.gd`
  (всегда в сборке) подгружает помощника только через `ResourceLoader.exists`,
  а в экспорте молча ничего не делает. Никакой код проекта к этой автозагрузке
  не обращается (проверено поиском), поэтому подмена безопасна;
- [ЗАБЛОКИРОВАНО — НУЖНО ДЕЙСТВИЕ ПОЛЬЗОВАТЕЛЯ] первый `--export-release`.
  Команда выполнена, пресет `Windows Desktop` найдён, каталог
  `build/windows/` создан (`build/` уже в `.gitignore` :14). Сборка остановилась
  не на проекте, а на отсутствии шаблонов экспорта:
  `C:/Users/litvi/AppData/Roaming/Godot/export_templates/4.7.stable/windows_release_x86_64.exe`
  не существует. Шаблоны ставятся только из редактора:
  «Редактор → Управление шаблонами экспорта → Скачать и установить» (~1 ГБ,
  версия ровно 4.7.stable). После этого повторить команду:
  `Godot_v4.7-stable_win64_console.exe --headless --path . --export-release
  "Windows Desktop" build/windows/MuseumLiminal.exe`;
- ЗАПУСТИТЬ собранный `.exe` руками и убедиться, что доходит до меню и
  до игры. До этого момента любая оценка готовности — догадка.

**Фаза 1 — одна ночь целиком и честно (вертикальный срез).**
Цель фазы — не красота, а ответ на вопрос «хочется ли вторую ночь».

- зафиксировать границы: СКОЛЬКО ночей, чем кончается игра, сколько длится
  одна ночь в минутах. Записать в этот файл до любого кода;
- ВСЕ ДЕСЯТЬ измерений остаются в выдаче инцидентов и остаются целью проекта.
  Правка от пользователя 2026-08-03: большой выбор квестов — это замысел, а не
  избыточность, и сокращать набор нельзя. Десять — нижняя граница, не верхняя;
- вместо сокращения набора вводится ПОРОГ КАЧЕСТВА: измерение считается годным,
  только если проходит чек-лист блока 8. Ночь среза выдаёт инциденты из всех
  десяти, и каждое встреченное измерение получает вердикт на месте;
- порядок доводки внутри фазы: три худших по итогам среза правятся первыми,
  потому что именно они портят впечатление от остальных семи;
- мид-сейв между ночами и пауза ночи внутри карманного измерения;
- сыграть ночь ЦЕЛИКОМ руками, в собранном билде, и записать посекундно,
  где скучно. Скука — это дефект такого же ранга, как вылет.

**Фаза 2 — звук.** Самый высокий возврат на вложенный час во всём проекте.

- фон поста (гул, лампы, вентиляция) и тишина как отдельный инструмент;
- голос угрозы: шаги Куратора с расстоянием и направлением — это дешевле,
  чем умный ИИ, а страху даёт больше;
- субтитры (ключи `ACCESS_SUBTITLES` уже есть в каталоге, читателей нет).

**Фаза 3 — курс картинки решён окончательно (блок 9).** До этого решения
блок 4 (текстуры) и блок 3 (двор) ЗАМОРОЖЕНЫ: нет смысла класть 2K-карты
туда, где через неделю будет 256 без сглаживания. Первые два пункта блока 9
(низкое разрешение и nearest) дают 90% вида за час работы и заодно закрывают
большую часть блока 5 (оптимизация).

**Фаза 4 — ревизия ВСЕХ десяти измерений (блок 8).**
Чек-лист из блока 8 остаётся без изменений — он хороший. Меняется только
вердикт: пункта «вырезать» больше нет. Слабое измерение переделывается, а не
удаляется, потому что широкий выбор квестов — заявленная цель проекта.
Работать по одному за заход, после каждого — `test_full_game_cycle` и
`test_incident_catalog`. Требование к набору: десять непохожих друг на друга
глаголов. Если два измерения играются одинаково, чинится не количество, а
повторяющееся из них.

**Фаза 5 — полировка и ширина.** Сюда уезжают блоки 3, 4, 6, 7, остаток 2
(деревья), оставшиеся окна стола и вес репозитория. Это не «неважно» —
это «дешевле после».

### Изменение в правилах приёмки (раздел 10)

К требованию «проверено глазами» добавляется второе: **всё, что касается
цикла ночи, считается закрытым только после того, как в это сыграли
руками в собранном билде.** Снимок доказывает вид, а не игру.

---

## 14. Ловушки, на которых уже теряли время

- Capture-тесты нельзя запускать с `--headless` — кадры выйдут пустыми.
- `file_read_media` тянет файлы примерно до 30 КБ. Кадры захвата — PNG по ~1.1 МБ,
  обязательно прогонять `python tools\tmp_shrink.py <папка>`. Но кадр 720×405 слишком мелкий,
  чтобы судить о мелкой детали: для тонкой оценки просить пользователя посмотреть в игре.
- `class_name` не регистрируется в голом `--script`-прогоне, использовать `preload`.
- `Plant3D` из PlantGenerator нельзя инстанцировать в игре — брать только L-системный движок.
- `rules` объявлять именно как `var r: Dictionary[String, String] = {...}`.
- Правки файлов только через `file_edit`; `patch_file` на этом сервере не существует.
- Командлеты PowerShell через `command_run` не работают — только `-File <script>.ps1`.
- Длинные цепочки команд в одном `command_run` ловят `MCP error -32001: Request timed out`.
  Захват кадров и прогон тестов запускать **раздельными** вызовами.
- `file_write` не создаёт каталоги по пути.
- `Object.tr()` недоступен из static-функции — `TranslationServer.translate(key)`.
- `Label3D` не билбордить.
- `test_full_game_cycle` печатает `ERROR: 3 resources still in use at exit` при `[PASS]` —
  это шум движка, не провал.
- `--import` ругается на dll terrabrush — норма, аддон в `project.godot` не включён.
